{
 "at": "v4471",
 "three": "0.178.0",
 "note": "the logistic map stepped `bound` times, the bound a vec4 uniform in one variant and a storage buffer's element in the other -- the first generated pass whose trip count is not baked into its text; render/physicsTsl.mjs makeLogisticStepperTsl, transplanted by render/tslSource.mjs",
 "emitted": "// Three.js r178 - Node System\n\n// directives\nenable subgroups;\n\n// system\nvar<private> instanceIndex : u32;\n\n// locals\n\n\n// structs\n\n\n// uniforms\n\nstruct x0Struct {\n\tvalue : array< f32 >\n};\n@binding( 0 ) @group( 0 )\nvar<storage, read_write> x0 : x0Struct;\n\nstruct rStruct {\n\tvalue : array< f32 >\n};\n@binding( 2 ) @group( 0 )\nvar<storage, read_write> r : rStruct;\n\nstruct outStruct {\n\tvalue : array< f32 >\n};\n@binding( 3 ) @group( 0 )\nvar<storage, read_write> out : outStruct;\nstruct objectStruct {\n\tbound : vec4<f32>\n};\n@binding( 1 ) @group( 0 )\nvar<uniform> object : objectStruct;\n\n// codes\n\n\n@compute @workgroup_size( 64 )\nfn main( @builtin( global_invocation_id ) globalId : vec3<u32>,\n\t@builtin( workgroup_id ) workgroupId : vec3<u32>,\n\t@builtin( local_invocation_id ) localId : vec3<u32>,\n\t@builtin( num_workgroups ) numWorkgroups : vec3<u32>,\n\t@builtin( subgroup_size ) subgroupSize : u32 ) {\n\n\t// system\n\tinstanceIndex = globalId.x + globalId.y * numWorkgroups.x * u32(64) + globalId.z * numWorkgroups.x * numWorkgroups.y * u32(64);\n\n\t// vars\n\t\n\tvar nodeVar0 : f32;\n\n\n\t// flow\n\t// code\n\n\tnodeVar0 = x0.value[ instanceIndex ];\n\n\tfor ( var i : i32 = 0; i < i32( object.bound.x ); i ++ ) {\n\n\t\tnodeVar0 = ( ( r.value[ instanceIndex ] * nodeVar0 ) * ( 1.0 - nodeVar0 ) );\n\n\t}\n\n\tout.value[ instanceIndex ] = nodeVar0;\n\n\t\n\n}\n",
 "transplanted": "// transplanted from three's WGSL compute builder by render/tslSource.mjs\nvar<private> instanceIndex : u32;\nstruct outBuf { value: array<f32> };\n@group(0) @binding(0) var<storage, read_write> out: outBuf;\nstruct rBuf { value: array<f32> };\n@group(0) @binding(1) var<storage, read> r: rBuf;\nstruct x0Buf { value: array<f32> };\n@group(0) @binding(2) var<storage, read> x0: x0Buf;\nstruct uStruct { bound: vec4<f32> };\n@group(0) @binding(3) var<uniform> u: uStruct;\n@compute @workgroup_size(64)\nfn main( @builtin( global_invocation_id ) globalId : vec3<u32>,\n\t@builtin( workgroup_id ) workgroupId : vec3<u32>,\n\t@builtin( local_invocation_id ) localId : vec3<u32>,\n\t@builtin( num_workgroups ) numWorkgroups : vec3<u32> ) {\n\n\t// system\n\tinstanceIndex = globalId.x + globalId.y * numWorkgroups.x * u32(64) + globalId.z * numWorkgroups.x * numWorkgroups.y * u32(64);\n\n\t// vars\n\t\n\tvar nodeVar0 : f32;\n\n\n\t// flow\n\t// code\n\n\tnodeVar0 = x0.value[ instanceIndex ];\n\n\tfor ( var i : i32 = 0; i < i32( u.bound.x ); i ++ ) {\n\n\t\tnodeVar0 = ( ( r.value[ instanceIndex ] * nodeVar0 ) * ( 1.0 - nodeVar0 ) );\n\n\t}\n\n\tout.value[ instanceIndex ] = nodeVar0;\n\n\t\n\n}\n",
 "emittedStorage": "// Three.js r178 - Node System\n\n// directives\nenable subgroups;\n\n// system\nvar<private> instanceIndex : u32;\n\n// locals\n\n\n// structs\n\n\n// uniforms\n\nstruct x0Struct {\n\tvalue : array< f32 >\n};\n@binding( 0 ) @group( 0 )\nvar<storage, read_write> x0 : x0Struct;\n\nstruct stepsStruct {\n\tvalue : array< f32 >\n};\n@binding( 1 ) @group( 0 )\nvar<storage, read_write> steps : stepsStruct;\n\nstruct rStruct {\n\tvalue : array< f32 >\n};\n@binding( 2 ) @group( 0 )\nvar<storage, read_write> r : rStruct;\n\nstruct outStruct {\n\tvalue : array< f32 >\n};\n@binding( 3 ) @group( 0 )\nvar<storage, read_write> out : outStruct;\n\n// codes\n\n\n@compute @workgroup_size( 64 )\nfn main( @builtin( global_invocation_id ) globalId : vec3<u32>,\n\t@builtin( workgroup_id ) workgroupId : vec3<u32>,\n\t@builtin( local_invocation_id ) localId : vec3<u32>,\n\t@builtin( num_workgroups ) numWorkgroups : vec3<u32>,\n\t@builtin( subgroup_size ) subgroupSize : u32 ) {\n\n\t// system\n\tinstanceIndex = globalId.x + globalId.y * numWorkgroups.x * u32(64) + globalId.z * numWorkgroups.x * numWorkgroups.y * u32(64);\n\n\t// vars\n\t\n\tvar nodeVar0 : f32;\n\n\n\t// flow\n\t// code\n\n\tnodeVar0 = x0.value[ instanceIndex ];\n\n\tfor ( var i : i32 = 0; i < i32( steps.value[ 0u ] ); i ++ ) {\n\n\t\tnodeVar0 = ( ( r.value[ instanceIndex ] * nodeVar0 ) * ( 1.0 - nodeVar0 ) );\n\n\t}\n\n\tout.value[ instanceIndex ] = nodeVar0;\n\n\t\n\n}\n",
 "transplantedStorage": "// transplanted from three's WGSL compute builder by render/tslSource.mjs\nvar<private> instanceIndex : u32;\nstruct outBuf { value: array<f32> };\n@group(0) @binding(0) var<storage, read_write> out: outBuf;\nstruct rBuf { value: array<f32> };\n@group(0) @binding(1) var<storage, read> r: rBuf;\nstruct x0Buf { value: array<f32> };\n@group(0) @binding(2) var<storage, read> x0: x0Buf;\nstruct stepsBuf { value: array<f32> };\n@group(0) @binding(3) var<storage, read> steps: stepsBuf;\n@compute @workgroup_size(64)\nfn main( @builtin( global_invocation_id ) globalId : vec3<u32>,\n\t@builtin( workgroup_id ) workgroupId : vec3<u32>,\n\t@builtin( local_invocation_id ) localId : vec3<u32>,\n\t@builtin( num_workgroups ) numWorkgroups : vec3<u32> ) {\n\n\t// system\n\tinstanceIndex = globalId.x + globalId.y * numWorkgroups.x * u32(64) + globalId.z * numWorkgroups.x * numWorkgroups.y * u32(64);\n\n\t// vars\n\t\n\tvar nodeVar0 : f32;\n\n\n\t// flow\n\t// code\n\n\tnodeVar0 = x0.value[ instanceIndex ];\n\n\tfor ( var i : i32 = 0; i < i32( steps.value[ 0u ] ); i ++ ) {\n\n\t\tnodeVar0 = ( ( r.value[ instanceIndex ] * nodeVar0 ) * ( 1.0 - nodeVar0 ) );\n\n\t}\n\n\tout.value[ instanceIndex ] = nodeVar0;\n\n\t\n\n}\n"
}

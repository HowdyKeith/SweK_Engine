{
 "at": "v4483",
 "three": "0.178.0",
 "note": "render/tslWide.mjs's quad graph (three computed varyings, one flat, the camera in the fragment) and planes pass as three's node builders emitted them and as render/tslSource.mjs transplanted them; rewritten by tools/ship/tslWide-selfcheck.mjs on every green run",
 "quad": {
  "wgsl": {
   "language": "wgsl",
   "vertex": "// Three.js r178 - Node System\n\n// directives\n\n\n// structs\n\n\n// uniforms\n\nstruct objectStruct {\n\tscale : vec2<f32>,\n\tnodeUniform5 : mat4x4<f32>\n};\n@binding( 1 ) @group( 1 )\nvar<uniform> object : objectStruct;\n\nstruct renderStruct {\n\tcameraProjectionMatrix : mat4x4<f32>,\n\tcameraViewMatrix : mat4x4<f32>\n};\n@binding( 1 ) @group( 0 )\nvar<uniform> render : renderStruct;\n\n// varyings\n\nstruct VaryingsStruct {\n\t@location( 3 ) vScaled : vec2<f32>,\n\t@location( 4 ) vLin : f32,\n\t@location( 5 ) @interpolate( flat ) vBand : i32,\n\t@builtin( position ) Vertex : vec4<f32>\n};\nvar<private> varyings : VaryingsStruct;\n\n// codes\n\n\n@vertex\nfn main( @location( 0 ) uv : vec2<f32>,\n\t@location( 1 ) position : vec3<f32> ) -> VaryingsStruct {\n\n\t// vars\n\t\n\tvar nodeVar0 : vec2<f32>;\n\tvar modelViewMatrix : mat4x4<f32>;\n\tvar nodeVar1 : vec4<f32>;\n\tvar v_modelViewProjection : vec4<f32>;\n\tvar v_positionView : vec3<f32>;\n\tvar positionLocal : vec3<f32>;\n\n\n\t// flow\n\t// code\n\n\tnodeVar0 = ( uv * object.scale );\n\tvaryings.vScaled = nodeVar0;\n\tpositionLocal = position;\n\tvaryings.vLin = ( ( ( positionLocal.x * 0.3 ) + ( positionLocal.y * 0.2 ) ) + 0.5 );\n\tvaryings.vBand = i32( floor( ( ( ( positionLocal.x - ( ( uv.x - 0.5 ) * 0.25 ) ) + 1.0 ) * 2.0 ) ) );\n\tmodelViewMatrix = ( render.cameraViewMatrix * object.nodeUniform5 );\n\tv_positionView = ( modelViewMatrix * vec4<f32>( positionLocal, 1.0 ) ).xyz;\n\tnodeVar1 = ( render.cameraProjectionMatrix * vec4<f32>( v_positionView, 1.0 ) );\n\tv_modelViewProjection = nodeVar1;\n\n\t// result\n\n\tvaryings.Vertex = v_modelViewProjection;\n\n\treturn varyings;\n\n}\n",
   "fragment": "// Three.js r178 - Node System\n\n// global\ndiagnostic( off, derivative_uniformity );\n\n\n// structs\n\nstruct OutputStruct {\n\t@location(0) color: vec4<f32>\n};\nvar<private> output : OutputStruct;\n\n// uniforms\n\nstruct renderStruct {\n\tcameraProjectionMatrix : mat4x4<f32>\n};\n@binding( 0 ) @group( 0 )\nvar<uniform> render : renderStruct;\n\nstruct objectStruct {\n\ttint : vec3<f32>\n};\n@binding( 0 ) @group( 1 )\nvar<uniform> object : objectStruct;\n\n// codes\n\n\n@fragment\nfn main( @location( 3 ) vScaled : vec2<f32>,\n\t@location( 4 ) vLin : f32,\n\t@location( 5 ) @interpolate( flat ) vBand : i32 ) -> OutputStruct {\n\n\t// vars\n\t\n\n\n\n\t// flow\n\t// code\n\n\n\t// result\n\n\toutput.color = ( vec4<f32>( vScaled.x, vLin, ( ( f32( ( vBand + 4 ) ) * 0.1 ) + ( ( render.cameraProjectionMatrix * vec4<f32>( vScaled, 0.0, 1.0 ) ).x * 0.05 ) ), 1.0 ) * vec4<f32>( object.tint, 1.0 ) );\n\n\treturn output;\n\n}\n"
  },
  "glsl": {
   "language": "glsl",
   "vertex": "#version 300 es\n\n// Three.js r178 - Node System\n\n\n// extensions\n\n\n// precision\n\nprecision highp float;\nprecision highp int;\nprecision highp sampler2D;\nprecision highp sampler3D;\nprecision highp samplerCube;\nprecision highp sampler2DArray;\n\nprecision highp usampler2D;\nprecision highp usampler3D;\nprecision highp usamplerCube;\nprecision highp usampler2DArray;\n\nprecision highp isampler2D;\nprecision highp isampler3D;\nprecision highp isamplerCube;\nprecision highp isampler2DArray;\n\nprecision lowp sampler2DShadow;\nprecision lowp sampler2DArrayShadow;\nprecision lowp samplerCubeShadow;\n\n\n// uniforms\n\nlayout( std140 ) uniform vertex_object {\n\tvec2 v_scale;\n\tmat4 v_nodeUniform5;\n};\n\nlayout( std140 ) uniform vertex_render {\n\tmat4 v_cameraProjectionMatrix;\n\tmat4 v_cameraViewMatrix;\n};\n\n\n// varyings\nvec4 v_modelViewProjection;\nvec3 v_positionView;\nvec3 positionLocal;\nout vec2 vScaled;\nout float vLin;\nflat  out int vBand;\n\n\n// attributes\nlayout( location = 0 ) in vec2 uv;\nlayout( location = 1 ) in vec3 position;\n\n\n// codes\n\n\nvoid main() {\n\n\t// vars\n\tvec2 nodeVar0;\n\tmat4 modelViewMatrix;\n\tvec4 nodeVar1;\n\n\t// transforms\n\t\n\n\t// flow\n\t// code\n\n\tnodeVar0 = ( uv * v_scale );\n\tvScaled = nodeVar0;\n\tpositionLocal = position;\n\tvLin = ( ( ( positionLocal.x * 0.3 ) + ( positionLocal.y * 0.2 ) ) + 0.5 );\n\tvBand = int( floor( ( ( ( positionLocal.x - ( ( uv.x - 0.5 ) * 0.25 ) ) + 1.0 ) * 2.0 ) ) );\n\tmodelViewMatrix = ( v_cameraViewMatrix * v_nodeUniform5 );\n\tv_positionView = ( modelViewMatrix * vec4( positionLocal, 1.0 ) ).xyz;\n\tnodeVar1 = ( v_cameraProjectionMatrix * vec4( v_positionView, 1.0 ) );\n\tv_modelViewProjection = nodeVar1;\n\n\t// result\n\tgl_Position = v_modelViewProjection;\n\n\tgl_PointSize = 1.0;\n\n}\n",
   "fragment": "#version 300 es\n\n// Three.js r178 - Node System\n\n\n// extensions\n\n\n// precision\n\nprecision highp float;\nprecision highp int;\nprecision highp sampler2D;\nprecision highp sampler3D;\nprecision highp samplerCube;\nprecision highp sampler2DArray;\n\nprecision highp usampler2D;\nprecision highp usampler3D;\nprecision highp usamplerCube;\nprecision highp usampler2DArray;\n\nprecision highp isampler2D;\nprecision highp isampler3D;\nprecision highp isamplerCube;\nprecision highp isampler2DArray;\n\nprecision lowp sampler2DShadow;\nprecision lowp sampler2DArrayShadow;\nprecision lowp samplerCubeShadow;\n\n\n// uniforms\n\nlayout( std140 ) uniform fragment_render {\n\tmat4 f_cameraProjectionMatrix;\n};\n\nlayout( std140 ) uniform fragment_object {\n\tvec3 f_tint;\n};\n\n\n// varyings\nin vec2 vScaled;\nin float vLin;\nflat  in int vBand;\n\n\n// codes\n\n\n// structs\n\nlayout( location = 0 ) out vec4 fragColor;\n\n\n\nvoid main() {\n\n\t// vars\n\t\n\n\t// flow\n\t// code\n\n\n\t// result\n\tfragColor = ( vec4( vScaled.x, vLin, ( ( float( ( vBand + 4 ) ) * 0.1 ) + ( ( f_cameraProjectionMatrix * vec4( vScaled, 0.0, 1.0 ) ).x * 0.05 ) ), 1.0 ) * vec4( f_tint, 1.0 ) );\n\n}\n"
  },
  "transplanted": {
   "wgsl": "// transplanted into the quad shell from three's WGSL node builder by render/tslSource.mjs\nstruct Cam { scale: vec2<f32>, tint: vec3<f32>, proj: mat4x4<f32> };\n@group(0) @binding(0) var<uniform> cam: Cam;\nstruct VOut { @builtin(position) pos: vec4<f32>, @location(0) uv: vec2<f32>, @location(1) vScaled: vec2<f32>, @location(2) vLin: f32, @location(3) @interpolate(flat) vBand: i32 };\n@vertex fn vs(@location(0) p: vec2<f32>, @location(1) uv: vec2<f32>) -> VOut {\n  var o: VOut; var pl = vec3<f32>(p, 0.0);\n  \n  o.pos = vec4<f32>(pl.xy, 0.0, 1.0); o.uv = uv;\n  var nodeVar0 : vec2<f32>;\n  nodeVar0 = ( uv * cam.scale );\n  o.vScaled = nodeVar0;\n  o.vLin = ( ( ( pl.x * 0.3 ) + ( pl.y * 0.2 ) ) + 0.5 );\n  o.vBand = i32( floor( ( ( ( pl.x - ( ( uv.x - 0.5 ) * 0.25 ) ) + 1.0 ) * 2.0 ) ) );\n  return o;\n}\n\n@fragment fn fs(v: VOut) -> @location(0) vec4<f32> {\n\n\t// vars\n\t\n\n\n\n\t// flow\n\t// code\n\n\n\t// result\n\n\treturn ( vec4<f32>( v.vScaled.x, v.vLin, ( ( f32( ( v.vBand + 4 ) ) * 0.1 ) + ( ( cam.proj * vec4<f32>( v.vScaled, 0.0, 1.0 ) ).x * 0.05 ) ), 1.0 ) * vec4<f32>( cam.tint, 1.0 ) );\n\n}\n",
   "glslVertex": "#version 300 es\nprecision highp float; precision highp int;\nuniform vec2 scale; uniform vec3 tint; uniform mat4 proj;\nin vec2 p; in vec2 uv;\nout vec2 vUv; out vec2 vScaled; out float vLin; flat out int vBand;\nvoid main() { vec3 pl = vec3(p, 0.0);\n  \n  gl_Position = vec4(pl.xy, 0.0, 1.0); vUv = uv;\n  vec2 nodeVar0;\n  nodeVar0 = ( uv * scale );\n  vScaled = nodeVar0;\n  vLin = ( ( ( pl.x * 0.3 ) + ( pl.y * 0.2 ) ) + 0.5 );\n  vBand = int( floor( ( ( ( pl.x - ( ( uv.x - 0.5 ) * 0.25 ) ) + 1.0 ) * 2.0 ) ) );\n}\n",
   "glslFragment": "#version 300 es\nprecision highp float; precision highp int;\nuniform vec2 scale; uniform vec3 tint; uniform mat4 proj;\nin vec2 vUv; in vec2 vScaled; in float vLin; flat in int vBand; out vec4 fragColor;\n\nvoid main() {\n\n\t// vars\n\t\n\n\t// flow\n\t// code\n\n\n\t// result\n\tfragColor = ( vec4( vScaled.x, vLin, ( ( float( ( vBand + 4 ) ) * 0.1 ) + ( ( proj * vec4( vScaled, 0.0, 1.0 ) ).x * 0.05 ) ), 1.0 ) * vec4( tint, 1.0 ) );\n\n}\n"
  }
 },
 "planes": {
  "wgsl": "// Three.js r178 - Node System\n\n// directives\nenable subgroups;\n\n// system\nvar<private> instanceIndex : u32;\n\n// locals\n\n\n// structs\n\n\n// uniforms\n\nstruct planesStruct {\n\tvalue : array< vec4<f32>, 6 >\n};\n@binding( 1 ) @group( 0 )\nvar<uniform> planes : planesStruct;\n\nstruct ptsStruct {\n\tvalue : array< vec4<f32> >\n};\n@binding( 2 ) @group( 0 )\nvar<storage, read_write> pts : ptsStruct;\n\nstruct distStruct {\n\tvalue : array< f32 >\n};\n@binding( 3 ) @group( 0 )\nvar<storage, read_write> dist : distStruct;\nstruct objectStruct {\n\tinfo : vec4<f32>\n};\n@binding( 0 ) @group( 0 )\nvar<uniform> object : objectStruct;\n\n// codes\n\n\n@compute @workgroup_size( 64 )\nfn main( @builtin( global_invocation_id ) globalId : vec3<u32>,\n\t@builtin( workgroup_id ) workgroupId : vec3<u32>,\n\t@builtin( local_invocation_id ) localId : vec3<u32>,\n\t@builtin( num_workgroups ) numWorkgroups : vec3<u32>,\n\t@builtin( subgroup_size ) subgroupSize : u32 ) {\n\n\t// system\n\tinstanceIndex = globalId.x + globalId.y * numWorkgroups.x * u32(64) + globalId.z * numWorkgroups.x * numWorkgroups.y * u32(64);\n\n\t// vars\n\t\n\tvar nodeVar0 : f32;\n\n\n\t// flow\n\t// code\n\n\n\tif ( ( f32( instanceIndex ) < object.info.x ) ) {\n\n\t\tnodeVar0 = 1000000000.0;\n\n\t\tfor ( var i : i32 = 0; i < 6; i ++ ) {\n\n\t\t\tnodeVar0 = min( nodeVar0, ( dot( planes.value[ i ].xyz, pts.value[ instanceIndex ].xyz ) + planes.value[ i ].w ) );\n\n\t\t}\n\n\t\tdist.value[ instanceIndex ] = nodeVar0;\n\t\t\n\n\t}\n\n\n\t\n\n}\n",
  "transplanted": "// transplanted from three's WGSL compute builder by render/tslSource.mjs\nvar<private> instanceIndex : u32;\nstruct ptsBuf { value: array<vec4<f32>> };\n@group(0) @binding(0) var<storage, read> pts: ptsBuf;\nstruct distBuf { value: array<f32> };\n@group(0) @binding(1) var<storage, read_write> dist: distBuf;\nstruct uStruct { planes: array<vec4<f32>, 6>, info: vec4<f32> };\n@group(0) @binding(2) var<uniform> u: uStruct;\n@compute @workgroup_size(64)\nfn main( @builtin( global_invocation_id ) globalId : vec3<u32>,\n\t@builtin( workgroup_id ) workgroupId : vec3<u32>,\n\t@builtin( local_invocation_id ) localId : vec3<u32>,\n\t@builtin( num_workgroups ) numWorkgroups : vec3<u32> ) {\n\n\t// system\n\tinstanceIndex = globalId.x + globalId.y * numWorkgroups.x * u32(64) + globalId.z * numWorkgroups.x * numWorkgroups.y * u32(64);\n\n\t// vars\n\t\n\tvar nodeVar0 : f32;\n\n\n\t// flow\n\t// code\n\n\n\tif ( ( f32( instanceIndex ) < u.info.x ) ) {\n\n\t\tnodeVar0 = 1000000000.0;\n\n\t\tfor ( var i : i32 = 0; i < 6; i ++ ) {\n\n\t\t\tnodeVar0 = min( nodeVar0, ( dot( u.planes[ i ].xyz, pts.value[ instanceIndex ].xyz ) + u.planes[ i ].w ) );\n\n\t\t}\n\n\t\tdist.value[ instanceIndex ] = nodeVar0;\n\t\t\n\n\t}\n\n\n\t\n\n}\n"
 }
}
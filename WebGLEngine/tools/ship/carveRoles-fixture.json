{
 "at": "v4372",
 "three": "0.178.0",
 "note": "Two minimal three-shaped compute shaders for tools/ship/carveGpu-selfcheck.mjs section 1, which asks whether render/tslSource.mjs's write-detector can tell a COMPARISON from an ASSIGNMENT. They live here rather than inline for the reason render/backendParity.mjs's header gives and this tree has relearned a dozen times: a file that spells a shader marker becomes a file the shader census counts, and a gate is not a shader-bearing module. tools/ship/tslCompute-fixture.json is the same decision at v4331.",
 "compare": "// Three.js r178 - Node System\nvar<private> instanceIndex : u32;\nstruct outStruct { value : array< u32 > };\n@binding( 0 ) @group( 0 )\nvar<storage, read_write> out : outStruct;\nstruct srcStruct { value : array< u32 > };\n@binding( 1 ) @group( 0 )\nvar<storage, read_write> src : srcStruct;\nstruct objectStruct { info : vec4<f32> };\n@binding( 2 ) @group( 0 )\nvar<uniform> object : objectStruct;\n@compute @workgroup_size( 64 )\nfn main( @builtin( global_invocation_id ) globalId : vec3<u32> ) {\n\tinstanceIndex = globalId.x;\n\tif ( src.value[ instanceIndex ] __TEST__ ) { out.value[ instanceIndex ] = 1u; }\n}\n",
 "assign": "// Three.js r178 - Node System\nvar<private> instanceIndex : u32;\nstruct outStruct { value : array< u32 > };\n@binding( 0 ) @group( 0 )\nvar<storage, read_write> out : outStruct;\nstruct srcStruct { value : array< u32 > };\n@binding( 1 ) @group( 0 )\nvar<storage, read_write> src : srcStruct;\nstruct objectStruct { info : vec4<f32> };\n@binding( 2 ) @group( 0 )\nvar<uniform> object : objectStruct;\n@compute @workgroup_size( 64 )\nfn main( @builtin( global_invocation_id ) globalId : vec3<u32> ) {\n\tinstanceIndex = globalId.x;\n\tsrc.value[ instanceIndex ] = 2u;\n\tout.value[ instanceIndex ] = 1u;\n}\n"
}

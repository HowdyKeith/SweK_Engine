{
 "at": "v4331",
 "three": "0.178.0",
 "note": "the Lyapunov sweep as a TSL compute pass, as three emitted it and as render/tslSource.mjs transplanted it into a gfx/device.js compute pipeline; rewritten by this gate on every run",
 "emitted": "// Three.js r178 - Node System\n\n// directives\nenable subgroups;\n\n// system\nvar<private> instanceIndex : u32;\n\n// locals\n\n\n// structs\n\n\n// uniforms\n\nstruct NodeBuffer_531Struct {\n\tvalue : array< f32 >\n};\n@binding( 0 ) @group( 0 )\nvar<storage, read_write> NodeBuffer_531 : NodeBuffer_531Struct;\nstruct objectStruct {\n\tspan : vec4<f32>\n};\n@binding( 1 ) @group( 0 )\nvar<uniform> object : objectStruct;\n\n// codes\n\n\n@compute @workgroup_size( 64 )\nfn main( @builtin( global_invocation_id ) globalId : vec3<u32>,\n\t@builtin( workgroup_id ) workgroupId : vec3<u32>,\n\t@builtin( local_invocation_id ) localId : vec3<u32>,\n\t@builtin( num_workgroups ) numWorkgroups : vec3<u32>,\n\t@builtin( subgroup_size ) subgroupSize : u32 ) {\n\n\t// system\n\tinstanceIndex = globalId.x + globalId.y * numWorkgroups.x * u32(64) + globalId.z * numWorkgroups.x * numWorkgroups.y * u32(64);\n\n\t// vars\n\t\n\tvar nodeVar0 : f32;\n\tvar nodeVar1 : f32;\n\tvar nodeVar2 : f32;\n\n\n\t// flow\n\t// code\n\n\tnodeVar0 = object.span.w;\n\n\tfor ( var i : i32 = 0; i < 64; i ++ ) {\n\n\t\tnodeVar1 = ( object.span.x + ( ( object.span.y - object.span.x ) * ( f32( instanceIndex ) / 63.0 ) ) );\n\t\tnodeVar0 = ( ( nodeVar1 * nodeVar0 ) * ( 1.0 - nodeVar0 ) );\n\n\t}\n\n\tnodeVar2 = 0.0;\n\n\tfor ( var i : i32 = 0; i < 384; i ++ ) {\n\n\t\tnodeVar2 = ( nodeVar2 + log( abs( ( nodeVar1 * ( 1.0 - ( nodeVar0 * 2.0 ) ) ) ) ) );\n\t\tnodeVar0 = ( ( nodeVar1 * nodeVar0 ) * ( 1.0 - nodeVar0 ) );\n\n\t}\n\n\tNodeBuffer_531.value[ instanceIndex ] = ( nodeVar2 / 384.0 );\n\n\t\n\n}\n",
 "transplanted": "// transplanted from three's WGSL compute builder by render/tslSource.mjs\nvar<private> instanceIndex : u32;\nstruct outBuf { value: array<f32> };\n@group(0) @binding(0) var<storage, read_write> out: outBuf;\nstruct uStruct { span: vec4<f32> };\n@group(0) @binding(1) var<uniform> u: uStruct;\n@compute @workgroup_size(64)\nfn main( @builtin( global_invocation_id ) globalId : vec3<u32>,\n\t@builtin( workgroup_id ) workgroupId : vec3<u32>,\n\t@builtin( local_invocation_id ) localId : vec3<u32>,\n\t@builtin( num_workgroups ) numWorkgroups : vec3<u32> ) {\n\n\t// system\n\tinstanceIndex = globalId.x + globalId.y * numWorkgroups.x * u32(64) + globalId.z * numWorkgroups.x * numWorkgroups.y * u32(64);\n\n\t// vars\n\t\n\tvar nodeVar0 : f32;\n\tvar nodeVar1 : f32;\n\tvar nodeVar2 : f32;\n\n\n\t// flow\n\t// code\n\n\tnodeVar0 = u.span.w;\n\n\tfor ( var i : i32 = 0; i < 64; i ++ ) {\n\n\t\tnodeVar1 = ( u.span.x + ( ( u.span.y - u.span.x ) * ( f32( instanceIndex ) / 63.0 ) ) );\n\t\tnodeVar0 = ( ( nodeVar1 * nodeVar0 ) * ( 1.0 - nodeVar0 ) );\n\n\t}\n\n\tnodeVar2 = 0.0;\n\n\tfor ( var i : i32 = 0; i < 384; i ++ ) {\n\n\t\tnodeVar2 = ( nodeVar2 + log( abs( ( nodeVar1 * ( 1.0 - ( nodeVar0 * 2.0 ) ) ) ) ) );\n\t\tnodeVar0 = ( ( nodeVar1 * nodeVar0 ) * ( 1.0 - nodeVar0 ) );\n\n\t}\n\n\tout.value[ instanceIndex ] = ( nodeVar2 / 384.0 );\n\n\t\n\n}\n"
}
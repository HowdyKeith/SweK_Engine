{
 "note": "v3126 -- orphan debt AS FOUND. A baseline, not an approval. NOTE: the first capture said 157; a naive block-comment stripper had eaten 71.7% of server.js and flagged 24 MOUNTED bridges as unreachable. Corrected to a line-wise stripper. A module reached only by its own selfcheck counts as unreachable (the meshMerge case) EXCEPT under tools/ship, where being run by a gate is the point. v3140: 61 engine/ modules were REMOVED by removeCluster (verified unreachable by resolution, iterated to convergence); their entries are dropped here because the files no longer exist. core/voxel.meshBuilder.js and gpu/gpu.meshUpload.js were EXPOSED by that removal -- their only importers were among the deleted -- and are recorded as known debt rather than deleted, because core/ and gpu/ were not the cluster under review. v3148: core/ and gpu/ cleared with Keith's explicit authorisation -- 35 modules removed (29 named by removeCluster plus a 6-file cascade the first pass exposed), verified unreachable BY RESOLUTION and iterated to convergence. Their entries are dropped because the files no longer exist. v3159: swept every area EXCEPT tools/ -- 163 modules removed across 20+ directories with Keith's authorisation, iterated to convergence. tools/ IS DELIBERATELY EXCLUDED: it holds HAND-RUN SCRIPTS (tools/export/captureHeadless.mjs and similar) which are entry points named NOWHERE -- not in HTML, not imported, invoked by typing them at a prompt -- so removeCluster cannot distinguish them from dead code. The gates themselves ARE protected, because verify.mjs names them as string literals. AND render/SSAOPass.js + simulation/SpatialHash.js WERE RESTORED after the sweep deleted them: orphanScan names both BY HAND as hand-verified examples, and SSAOPass is explicitly marked A REVIVE CANDIDATE, NOT DEAD WEIGHT. A file somebody deliberately flagged as worth keeping is not an orphan to sweep, and only the gate knew. v3176: ui/knownState.js RESTORED from the v3158 zip -- the v3159 sweep DELETED IT. It is v3113s adopt-me primitive for round-trip controls, with only 2 adopters, so it looked unreachable; unknownNotDefault-selfcheck has been RED for 17 versions because verify does not run it. A MODULE BUILT TO BE ADOPTED LOOKS EXACTLY LIKE ONE NOBODY WANTED. v3187: physics/spacefill/hilbert.mjs is UNWIRED BY DESIGN AND SAID SO IN ITS OWN CHANGELOG ENTRY -- it is the deterministic form of SLDgen s single-continuous-stroke idea, built as a candidate lab device with the raster scan as its load-bearing negative. A MODULE BUILT TO BE ADOPTED LOOKS EXACTLY LIKE ONE NOBODY WANTED (v3176, which is how the v3159 sweep deleted ui/knownState.js), so it is listed HERE WITH THE REASON rather than left for a sweep to find. v3195: physics/spacefill/hilbert.mjs REMOVED -- v3187 baselined it as unwired-by-design and v3188 WIRED IT into spacefillBind.mjs, so the entry outlived its reason by seven rounds. A BASELINE ENTRY WHOSE REASON HAS EXPIRED IS A RATCHET HOLDING NOTHING, and nothing would have caught it: orphanScan does not complain about a listed file that stopped being an orphan. v3202: FIVE ENTRIES REMOVED because they no longer describe an orphan -- policyCache, rl/memoryPolicy, multigridGPU, RagdollDismember and ui/knownState all became reachable while their suppressions sat here. A STALE SUPPRESSION IS AN ACTIVE BLIND SPOT: if one of those becomes a real orphan again, the entry SILENTLY SWALLOWS IT, and nobody audits a list they believe is working. ui/knownState.js was never an orphan at all -- main.js imports it THREE TIMES and I baselined it anyway at v3176. THE RIGHT RESPONSE TO A STALE ENTRY IS DELETION, NOT A LOOSENED CHECK. Now enforced by tools/ship/baselineHygiene-selfcheck.mjs.  v3900: suspiciousZero.mjs and waterMath.js DELETED -- both genuinely adopted (main.js imports both; waterMath also from physics/em/fresnelJoin.mjs and physics/fluid/gravityWaves.mjs). A suppression stops describing an orphan because the module got ADOPTED, which is progress, and progress is recorded by ceasing to suppress. haDiscovery.js and SSAOPass.js were ALSO reported stale and are KEPT: both are still orphans, marked reached only by prose STRINGS that describe them as orphans -- server.html's tooltip literally reads \"SSAOPass (ORPHAN, no gate...)\". Deleting those two would unprotect files the v3159 sweep already took once. v3912 -- ai-bridge/haDiscovery.js and render/SSAOPass.js REMOVED: both stopped being orphans, which is PROGRESS, and the fix baselineHygiene names for that is DELETION rather than loosening. Removing the entry is how the tree records that the debt was paid; keeping a note that refers to nothing is how a suppression outlives its reason. v4009 -- v3912 WAS WRONG ABOUT SSAOPass.js, AND v3900'S OWN NOTE ALREADY SAID SO TWELVE VERSIONS EARLIER: 'marked reached only by prose STRINGS that describe them as orphans -- server.html's tooltip literally reads SSAOPass (ORPHAN, no gate...)'. orphanScan.mjs had no CODE enforcing that warning, only the narrative, so the next baselineHygiene run read the same prose mention as real progress and deleted the entry anyway. Traced to TWO prose sources: render/ssaoCompare.mjs's data table (`file: \"render/SSAOPass.js\"`, never loaded) and server.html's tooltip title attribute on the /ssao-compare.html link -- both fixed structurally in orphanScan.mjs (a REPORT_MODULE exclusion list for hand-written report modules, and a general title-attribute strip), not by re-adding this baseline entry alone. render/SSAOPass.js RESTORED here because it is, independently of all of that, still what v3159 and v3900 already established: a real orphan and a REVIVE CANDIDATE, NOT DEAD WEIGHT. ai-bridge/haDiscovery.js is NOT restored -- its non-orphan status is legitimate, via duplicate-files-baseline.json, a GENERATED record already excluded by isGeneratedRecord since v3900. v4153: ui/avatarExpression.js recorded as known debt, and the REASON IS THE FINDING. v4112 built it as the one owner joining ui/faceExpressionSet.js (a named expression from a face) to face/avatarStage.js's setMorph() -- and THE TWO HALVES HAVE NEVER BEEN ON THE SAME PAGE. attachAvatarExpression(stage, reader, getSnap) needs both, but measured: the four pages carrying an avatar stage (krbn-compare, pipboy-models, avatarstage, phone) track no face at all, and face-mirror.html -- the one page with the expression reader -- has no avatar stage. So this is NOT a missing import somebody forgot; wiring it means putting a GLB stage on the mirror page or a camera tracker on a stage page, which is a real feature needing a webcam and a GPU to look at. It is NOT registered in DIRECTORY_LOADED, because that would claim a loading mechanism that does not exist -- the module genuinely is unreached. Same call as this gate already makes about render/SSAOPass.js: the integration point exists, the change is visible, and it belongs on the rig rather than in a blind edit.",
 "captured": "2026-08-25",
 "count": 7,
 "files": [
  "brain/cs/csEnv.js",
  "render/SSAOPass.js",
  "simulation/SpatialHash.js",
  "tools/facePlacementSystem.js",
  "tools/selectionState.js",
  "tools/voxelToolSystem.js",
  "ui/avatarExpression.js"
 ]
}
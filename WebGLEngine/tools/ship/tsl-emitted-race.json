{
 "at": "v4322",
 "three": "0.178.0",
 "note": "the Lyapunov look as three's node builders emitted it from render/physicsTsl.mjs makeLyapunovLookTsl, and as render/tslSource.mjs transplanted it into the look's own shell; rewritten by tools/ship/tslRace-selfcheck.mjs on every run",
 "wgsl": {
  "language": "wgsl",
  "vertex": "// Three.js r178 - Node System\n\n// directives\n\n\n// structs\n\n\n// uniforms\n\nstruct renderStruct {\n\tcameraProjectionMatrix : mat4x4<f32>,\n\tcameraViewMatrix : mat4x4<f32>\n};\n@binding( 0 ) @group( 0 )\nvar<uniform> render : renderStruct;\n\nstruct objectStruct {\n\tnodeUniform4 : mat4x4<f32>\n};\n@binding( 1 ) @group( 1 )\nvar<uniform> object : objectStruct;\n\n// varyings\n\nstruct VaryingsStruct {\n\t@location( 3 ) nodeVarying3 : vec2<f32>,\n\t@location( 4 ) nodeVarying4 : vec3<f32>,\n\t@location( 5 ) nodeVarying5 : vec4<f32>,\n\t@builtin( position ) Vertex : vec4<f32>\n};\nvar<private> varyings : VaryingsStruct;\n\n// codes\n\n\n@vertex\nfn main( @location( 0 ) uv : vec2<f32>,\n\t@location( 1 ) normal : vec3<f32>,\n\t@location( 2 ) color : vec4<f32>,\n\t@location( 3 ) position : vec3<f32> ) -> VaryingsStruct {\n\n\t// vars\n\t\n\tvar modelViewMatrix : mat4x4<f32>;\n\tvar nodeVar3 : vec4<f32>;\n\tvar v_modelViewProjection : vec4<f32>;\n\tvar v_positionView : vec3<f32>;\n\tvar positionLocal : vec3<f32>;\n\n\n\t// flow\n\t// code\n\n\tvaryings.nodeVarying3 = uv;\n\tvaryings.nodeVarying4 = normal;\n\tvaryings.nodeVarying5 = color;\n\tpositionLocal = position;\n\tmodelViewMatrix = ( render.cameraViewMatrix * object.nodeUniform4 );\n\tv_positionView = ( modelViewMatrix * vec4<f32>( positionLocal, 1.0 ) ).xyz;\n\tnodeVar3 = ( render.cameraProjectionMatrix * vec4<f32>( v_positionView, 1.0 ) );\n\tv_modelViewProjection = nodeVar3;\n\n\t// result\n\n\tvaryings.Vertex = v_modelViewProjection;\n\n\treturn varyings;\n\n}\n",
  "fragment": "// Three.js r178 - Node System\n\n// global\ndiagnostic( off, derivative_uniformity );\n\n\n// structs\n\nstruct OutputStruct {\n\t@location(0) color: vec4<f32>\n};\nvar<private> output : OutputStruct;\n\n// uniforms\n\nstruct objectStruct {\n\tchaos : vec4<f32>,\n\tlight : vec4<f32>\n};\n@binding( 0 ) @group( 1 )\nvar<uniform> object : objectStruct;\n\n// codes\n\n\n@fragment\nfn main( @location( 3 ) nodeVarying3 : vec2<f32>,\n\t@location( 4 ) nodeVarying4 : vec3<f32>,\n\t@location( 5 ) nodeVarying5 : vec4<f32> ) -> OutputStruct {\n\n\t// vars\n\t\n\tvar nodeVar0 : f32;\n\tvar nodeVar1 : f32;\n\tvar nodeVar2 : f32;\n\tvar normalLocal : vec3<f32>;\n\n\n\t// flow\n\t// code\n\n\tnodeVar0 = ( 0.05 + ( clamp( ( ( nodeVarying3.y * 0.5 ) + 0.5 ), 0.0, 1.0 ) * 0.9 ) );\n\n\tfor ( var i : i32 = 0; i < 32; i ++ ) {\n\n\t\tnodeVar1 = ( object.chaos.x + ( ( object.chaos.y - object.chaos.x ) * clamp( ( ( nodeVarying3.x * 0.5 ) + 0.5 ), 0.0, 1.0 ) ) );\n\t\tnodeVar0 = ( ( nodeVar1 * nodeVar0 ) * ( 1.0 - nodeVar0 ) );\n\n\t}\n\n\tnodeVar2 = 0.0;\n\n\tfor ( var i : i32 = 0; i < 96; i ++ ) {\n\n\t\tnodeVar2 = ( nodeVar2 + log( abs( ( nodeVar1 * ( 1.0 - ( nodeVar0 * 2.0 ) ) ) ) ) );\n\t\tnodeVar0 = ( ( nodeVar1 * nodeVar0 ) * ( 1.0 - nodeVar0 ) );\n\n\t}\n\n\tnormalLocal = nodeVarying4;\n\n\t// result\n\n\toutput.color = vec4<f32>( ( ( mix( vec3<f32>( 0.08, 0.06, 0.2 ), vec3<f32>( 0.35, 0.95, 0.85 ), max( clamp( ( ( nodeVar2 / 96.0 ) / 0.6931471805599453 ), -1.0, 1.0 ), 0.0 ) ) * vec3<f32>( ( object.light.w + ( ( 1.0 - object.light.w ) * max( dot( normalize( normalLocal ), normalize( object.light.xyz ) ), 0.0 ) ) ) ) ) * nodeVarying5.xyz ), nodeVarying5.w );\n\n\treturn output;\n\n}\n"
 },
 "glsl": {
  "language": "glsl",
  "vertex": "#version 300 es\n\n// Three.js r178 - Node System\n\n\n// extensions\n\n\n// precision\n\nprecision highp float;\nprecision highp int;\nprecision highp sampler2D;\nprecision highp sampler3D;\nprecision highp samplerCube;\nprecision highp sampler2DArray;\n\nprecision highp usampler2D;\nprecision highp usampler3D;\nprecision highp usamplerCube;\nprecision highp usampler2DArray;\n\nprecision highp isampler2D;\nprecision highp isampler3D;\nprecision highp isamplerCube;\nprecision highp isampler2DArray;\n\nprecision lowp sampler2DShadow;\nprecision lowp sampler2DArrayShadow;\nprecision lowp samplerCubeShadow;\n\n\n// uniforms\n\nlayout( std140 ) uniform vertex_render {\n\tmat4 v_cameraProjectionMatrix;\n\tmat4 v_cameraViewMatrix;\n};\n\nlayout( std140 ) uniform vertex_object {\n\tmat4 v_nodeUniform4;\n};\n\n\n// varyings\nvec4 v_modelViewProjection;\nvec3 v_positionView;\nvec3 positionLocal;\nout vec2 nodeVarying3;\nout vec3 nodeVarying4;\nout vec4 nodeVarying5;\n\n\n// attributes\nlayout( location = 0 ) in vec2 uv;\nlayout( location = 1 ) in vec3 normal;\nlayout( location = 2 ) in vec4 color;\nlayout( location = 3 ) in vec3 position;\n\n\n// codes\n\n\nvoid main() {\n\n\t// vars\n\tmat4 modelViewMatrix;\n\tvec4 nodeVar3;\n\n\t// transforms\n\t\n\n\t// flow\n\t// code\n\n\tnodeVarying3 = uv;\n\tnodeVarying4 = normal;\n\tnodeVarying5 = color;\n\tpositionLocal = position;\n\tmodelViewMatrix = ( v_cameraViewMatrix * v_nodeUniform4 );\n\tv_positionView = ( modelViewMatrix * vec4( positionLocal, 1.0 ) ).xyz;\n\tnodeVar3 = ( v_cameraProjectionMatrix * vec4( v_positionView, 1.0 ) );\n\tv_modelViewProjection = nodeVar3;\n\n\t// result\n\tgl_Position = v_modelViewProjection;\n\n\tgl_PointSize = 1.0;\n\n}\n",
  "fragment": "#version 300 es\n\n// Three.js r178 - Node System\n\n\n// extensions\n\n\n// precision\n\nprecision highp float;\nprecision highp int;\nprecision highp sampler2D;\nprecision highp sampler3D;\nprecision highp samplerCube;\nprecision highp sampler2DArray;\n\nprecision highp usampler2D;\nprecision highp usampler3D;\nprecision highp usamplerCube;\nprecision highp usampler2DArray;\n\nprecision highp isampler2D;\nprecision highp isampler3D;\nprecision highp isamplerCube;\nprecision highp isampler2DArray;\n\nprecision lowp sampler2DShadow;\nprecision lowp sampler2DArrayShadow;\nprecision lowp samplerCubeShadow;\n\n\n// uniforms\n\nlayout( std140 ) uniform fragment_object {\n\tvec4 f_chaos;\n\tvec4 f_light;\n};\n\n\n// varyings\nin vec2 nodeVarying3;\nin vec3 nodeVarying4;\nin vec4 nodeVarying5;\n\n\n// codes\n\n\n// structs\n\nlayout( location = 0 ) out vec4 fragColor;\n\n\n\nvoid main() {\n\n\t// vars\n\tfloat nodeVar0;\n\tfloat nodeVar1;\n\tfloat nodeVar2;\n\tvec3 normalLocal;\n\n\t// flow\n\t// code\n\n\tnodeVar0 = ( 0.05 + ( clamp( ( ( nodeVarying3.y * 0.5 ) + 0.5 ), 0.0, 1.0 ) * 0.9 ) );\n\n\tfor ( int i = 0; i < 32; i ++ ) {\n\n\t\tnodeVar1 = ( f_chaos.x + ( ( f_chaos.y - f_chaos.x ) * clamp( ( ( nodeVarying3.x * 0.5 ) + 0.5 ), 0.0, 1.0 ) ) );\n\t\tnodeVar0 = ( ( nodeVar1 * nodeVar0 ) * ( 1.0 - nodeVar0 ) );\n\n\t}\n\n\tnodeVar2 = 0.0;\n\n\tfor ( int i = 0; i < 96; i ++ ) {\n\n\t\tnodeVar2 = ( nodeVar2 + log( abs( ( nodeVar1 * ( 1.0 - ( nodeVar0 * 2.0 ) ) ) ) ) );\n\t\tnodeVar0 = ( ( nodeVar1 * nodeVar0 ) * ( 1.0 - nodeVar0 ) );\n\n\t}\n\n\tnormalLocal = nodeVarying4;\n\n\t// result\n\tfragColor = vec4( ( ( mix( vec3( 0.08, 0.06, 0.2 ), vec3( 0.35, 0.95, 0.85 ), max( clamp( ( ( nodeVar2 / 96.0 ) / 0.6931471805599453 ), -1.0, 1.0 ), 0.0 ) ) * vec3( ( f_light.w + ( ( 1.0 - f_light.w ) * max( dot( normalize( normalLocal ), normalize( f_light.xyz ) ), 0.0 ) ) ) ) ) * nodeVarying5.xyz ), nodeVarying5.w );\n\n}\n"
 },
 "transplanted": {
  "wgsl": "// transplanted into the lyapunov look shell from three's WGSL node builder by render/tslSource.mjs\nstruct Cam { viewProj: mat4x4<f32>, light: vec4<f32>, chaos: vec4<f32> };\n@group(0) @binding(0) var<uniform> cam: Cam;\nfn turned(p: vec3<f32>, yaw: f32) -> vec3<f32> {\n  let ca = cos(yaw); let sa = sin(yaw);\n  return vec3<f32>(p.x * ca - p.y * sa, p.x * sa + p.y * ca, p.z);\n}\nstruct VOut { @builtin(position) pos: vec4<f32>, @location(0) color: vec4<f32>, @location(1) n: vec3<f32>, @location(2) local: vec2<f32> };\n@vertex fn vs(@location(0) p: vec3<f32>, @location(1) color: vec4<f32>, @location(2) rec: vec4<f32>, @location(3) ident: vec4<f32>, @location(5) extra: vec4<f32>, @location(4) n: vec3<f32>) -> VOut {\n  var o: VOut; var pl = p; var nl = n;\n  \n  o.pos = cam.viewProj * vec4<f32>(rec.xyz + turned(pl, extra.x) * rec.w, 1.0);\n  o.color = color; o.n = turned(n, extra.x); o.local = p.xy;\n  return o;\n}\n\n@fragment fn fs(v: VOut) -> @location(0) vec4<f32> {\n\n\t// vars\n\t\n\tvar nodeVar0 : f32;\n\tvar nodeVar1 : f32;\n\tvar nodeVar2 : f32;\n\tvar normalLocal : vec3<f32>;\n\n\n\t// flow\n\t// code\n\n\tnodeVar0 = ( 0.05 + ( clamp( ( ( v.local.y * 0.5 ) + 0.5 ), 0.0, 1.0 ) * 0.9 ) );\n\n\tfor ( var i : i32 = 0; i < 32; i ++ ) {\n\n\t\tnodeVar1 = ( cam.chaos.x + ( ( cam.chaos.y - cam.chaos.x ) * clamp( ( ( v.local.x * 0.5 ) + 0.5 ), 0.0, 1.0 ) ) );\n\t\tnodeVar0 = ( ( nodeVar1 * nodeVar0 ) * ( 1.0 - nodeVar0 ) );\n\n\t}\n\n\tnodeVar2 = 0.0;\n\n\tfor ( var i : i32 = 0; i < 96; i ++ ) {\n\n\t\tnodeVar2 = ( nodeVar2 + log( abs( ( nodeVar1 * ( 1.0 - ( nodeVar0 * 2.0 ) ) ) ) ) );\n\t\tnodeVar0 = ( ( nodeVar1 * nodeVar0 ) * ( 1.0 - nodeVar0 ) );\n\n\t}\n\n\tnormalLocal = v.n;\n\n\t// result\n\n\treturn vec4<f32>( ( ( mix( vec3<f32>( 0.08, 0.06, 0.2 ), vec3<f32>( 0.35, 0.95, 0.85 ), max( clamp( ( ( nodeVar2 / 96.0 ) / 0.6931471805599453 ), -1.0, 1.0 ), 0.0 ) ) * vec3<f32>( ( cam.light.w + ( ( 1.0 - cam.light.w ) * max( dot( normalize( normalLocal ), normalize( cam.light.xyz ) ), 0.0 ) ) ) ) ) * v.color.xyz ), v.color.w );\n\n}\n",
  "glsl": "#version 300 es\nprecision highp float;\nuniform mat4 viewProj; uniform vec4 light; uniform vec4 chaos;\nin vec4 vColor; in vec3 vN; in vec2 vLocal; out vec4 fragColor;\n\nvoid main() {\n\n\t// vars\n\tfloat nodeVar0;\n\tfloat nodeVar1;\n\tfloat nodeVar2;\n\tvec3 normalLocal;\n\n\t// flow\n\t// code\n\n\tnodeVar0 = ( 0.05 + ( clamp( ( ( vLocal.y * 0.5 ) + 0.5 ), 0.0, 1.0 ) * 0.9 ) );\n\n\tfor ( int i = 0; i < 32; i ++ ) {\n\n\t\tnodeVar1 = ( chaos.x + ( ( chaos.y - chaos.x ) * clamp( ( ( vLocal.x * 0.5 ) + 0.5 ), 0.0, 1.0 ) ) );\n\t\tnodeVar0 = ( ( nodeVar1 * nodeVar0 ) * ( 1.0 - nodeVar0 ) );\n\n\t}\n\n\tnodeVar2 = 0.0;\n\n\tfor ( int i = 0; i < 96; i ++ ) {\n\n\t\tnodeVar2 = ( nodeVar2 + log( abs( ( nodeVar1 * ( 1.0 - ( nodeVar0 * 2.0 ) ) ) ) ) );\n\t\tnodeVar0 = ( ( nodeVar1 * nodeVar0 ) * ( 1.0 - nodeVar0 ) );\n\n\t}\n\n\tnormalLocal = vN;\n\n\t// result\n\tfragColor = vec4( ( ( mix( vec3( 0.08, 0.06, 0.2 ), vec3( 0.35, 0.95, 0.85 ), max( clamp( ( ( nodeVar2 / 96.0 ) / 0.6931471805599453 ), -1.0, 1.0 ), 0.0 ) ) * vec3( ( light.w + ( ( 1.0 - light.w ) * max( dot( normalize( normalLocal ), normalize( light.xyz ) ), 0.0 ) ) ) ) ) * vColor.xyz ), vColor.w );\n\n}\n"
 },
 "sprite": {
  "note": "v4325 -- the Heidler sprite look as three's builders emitted it, and as tslSource transplanted it into the sprite shell",
  "wgsl": {
   "language": "wgsl",
   "vertex": "// Three.js r178 - Node System\n\n// directives\n\n\n// structs\n\n\n// uniforms\n\nstruct renderStruct {\n\tcameraProjectionMatrix : mat4x4<f32>,\n\tcameraViewMatrix : mat4x4<f32>\n};\n@binding( 0 ) @group( 0 )\nvar<uniform> render : renderStruct;\n\nstruct objectStruct {\n\tnodeUniform4 : mat4x4<f32>\n};\n@binding( 1 ) @group( 1 )\nvar<uniform> object : objectStruct;\n\n// varyings\n\nstruct VaryingsStruct {\n\t@location( 3 ) nodeVarying3 : vec2<f32>,\n\t@location( 4 ) nodeVarying4 : vec4<f32>,\n\t@builtin( position ) Vertex : vec4<f32>\n};\nvar<private> varyings : VaryingsStruct;\n\n// codes\n\n\n@vertex\nfn main( @location( 0 ) uv : vec2<f32>,\n\t@location( 1 ) color : vec4<f32>,\n\t@location( 2 ) position : vec3<f32> ) -> VaryingsStruct {\n\n\t// vars\n\t\n\tvar modelViewMatrix : mat4x4<f32>;\n\tvar nodeVar3 : vec4<f32>;\n\tvar v_modelViewProjection : vec4<f32>;\n\tvar v_positionView : vec3<f32>;\n\tvar positionLocal : vec3<f32>;\n\n\n\t// flow\n\t// code\n\n\tvaryings.nodeVarying3 = uv;\n\tvaryings.nodeVarying4 = color;\n\tpositionLocal = position;\n\tmodelViewMatrix = ( render.cameraViewMatrix * object.nodeUniform4 );\n\tv_positionView = ( modelViewMatrix * vec4<f32>( positionLocal, 1.0 ) ).xyz;\n\tnodeVar3 = ( render.cameraProjectionMatrix * vec4<f32>( v_positionView, 1.0 ) );\n\tv_modelViewProjection = nodeVar3;\n\n\t// result\n\n\tvaryings.Vertex = v_modelViewProjection;\n\n\treturn varyings;\n\n}\n",
   "fragment": "// Three.js r178 - Node System\n\n// global\ndiagnostic( off, derivative_uniformity );\n\n\n// structs\n\nstruct OutputStruct {\n\t@location(0) color: vec4<f32>\n};\nvar<private> output : OutputStruct;\n\n// uniforms\n\nstruct objectStruct {\n\tbolt : vec4<f32>,\n\tspan : vec4<f32>\n};\n@binding( 0 ) @group( 1 )\nvar<uniform> object : objectStruct;\n\n// codes\n\n\n@fragment\nfn main( @location( 3 ) nodeVarying3 : vec2<f32>,\n\t@location( 4 ) nodeVarying4 : vec4<f32> ) -> OutputStruct {\n\n\t// vars\n\t\n\tvar nodeVar0 : f32;\n\tvar nodeVar1 : f32;\n\tvar nodeVar2 : f32;\n\n\n\t// flow\n\t// code\n\n\n\tif ( ( ( object.span.x * exp( ( log( ( object.span.y / object.span.x ) ) * nodeVarying3.x ) ) ) <= 0.0 ) ) {\n\n\t\tnodeVar0 = 0.0;\n\n\t} else {\n\n\t\tnodeVar1 = ( object.span.x * exp( ( log( ( object.span.y / object.span.x ) ) * nodeVarying3.x ) ) );\n\t\tnodeVar2 = ( ( nodeVar1 / object.bolt.y ) * ( nodeVar1 / object.bolt.y ) );\n\t\tnodeVar0 = ( ( nodeVar2 / ( 1.0 + nodeVar2 ) ) * exp( ( ( - nodeVar1 ) / object.bolt.z ) ) );\n\n\t}\n\n\n\t// result\n\n\toutput.color = vec4<f32>( ( mix( vec3<f32>( 0.05, 0.05, 0.16 ), vec3<f32>( 1.0, 0.95, 0.7 ), ( clamp( ( ( ( object.bolt.x / object.bolt.w ) * nodeVar0 ) / object.bolt.x ), 0.0, 1.0 ) * clamp( ( 1.0 - abs( ( ( nodeVarying3.y * 2.0 ) - 1.0 ) ) ), 0.0, 1.0 ) ) ) * nodeVarying4.xyz ), nodeVarying4.w );\n\n\treturn output;\n\n}\n"
  },
  "glsl": {
   "language": "glsl",
   "vertex": "#version 300 es\n\n// Three.js r178 - Node System\n\n\n// extensions\n\n\n// precision\n\nprecision highp float;\nprecision highp int;\nprecision highp sampler2D;\nprecision highp sampler3D;\nprecision highp samplerCube;\nprecision highp sampler2DArray;\n\nprecision highp usampler2D;\nprecision highp usampler3D;\nprecision highp usamplerCube;\nprecision highp usampler2DArray;\n\nprecision highp isampler2D;\nprecision highp isampler3D;\nprecision highp isamplerCube;\nprecision highp isampler2DArray;\n\nprecision lowp sampler2DShadow;\nprecision lowp sampler2DArrayShadow;\nprecision lowp samplerCubeShadow;\n\n\n// uniforms\n\nlayout( std140 ) uniform vertex_render {\n\tmat4 v_cameraProjectionMatrix;\n\tmat4 v_cameraViewMatrix;\n};\n\nlayout( std140 ) uniform vertex_object {\n\tmat4 v_nodeUniform4;\n};\n\n\n// varyings\nvec4 v_modelViewProjection;\nvec3 v_positionView;\nvec3 positionLocal;\nout vec2 nodeVarying3;\nout vec4 nodeVarying4;\n\n\n// attributes\nlayout( location = 0 ) in vec2 uv;\nlayout( location = 1 ) in vec4 color;\nlayout( location = 2 ) in vec3 position;\n\n\n// codes\n\n\nvoid main() {\n\n\t// vars\n\tmat4 modelViewMatrix;\n\tvec4 nodeVar3;\n\n\t// transforms\n\t\n\n\t// flow\n\t// code\n\n\tnodeVarying3 = uv;\n\tnodeVarying4 = color;\n\tpositionLocal = position;\n\tmodelViewMatrix = ( v_cameraViewMatrix * v_nodeUniform4 );\n\tv_positionView = ( modelViewMatrix * vec4( positionLocal, 1.0 ) ).xyz;\n\tnodeVar3 = ( v_cameraProjectionMatrix * vec4( v_positionView, 1.0 ) );\n\tv_modelViewProjection = nodeVar3;\n\n\t// result\n\tgl_Position = v_modelViewProjection;\n\n\tgl_PointSize = 1.0;\n\n}\n",
   "fragment": "#version 300 es\n\n// Three.js r178 - Node System\n\n\n// extensions\n\n\n// precision\n\nprecision highp float;\nprecision highp int;\nprecision highp sampler2D;\nprecision highp sampler3D;\nprecision highp samplerCube;\nprecision highp sampler2DArray;\n\nprecision highp usampler2D;\nprecision highp usampler3D;\nprecision highp usamplerCube;\nprecision highp usampler2DArray;\n\nprecision highp isampler2D;\nprecision highp isampler3D;\nprecision highp isamplerCube;\nprecision highp isampler2DArray;\n\nprecision lowp sampler2DShadow;\nprecision lowp sampler2DArrayShadow;\nprecision lowp samplerCubeShadow;\n\n\n// uniforms\n\nlayout( std140 ) uniform fragment_object {\n\tvec4 f_bolt;\n\tvec4 f_span;\n};\n\n\n// varyings\nin vec2 nodeVarying3;\nin vec4 nodeVarying4;\n\n\n// codes\n\n\n// structs\n\nlayout( location = 0 ) out vec4 fragColor;\n\n\n\nvoid main() {\n\n\t// vars\n\tfloat nodeVar0;\n\tfloat nodeVar1;\n\tfloat nodeVar2;\n\n\t// flow\n\t// code\n\n\n\tif ( ( ( f_span.x * exp( ( log( ( f_span.y / f_span.x ) ) * nodeVarying3.x ) ) ) <= 0.0 ) ) {\n\n\t\tnodeVar0 = 0.0;\n\n\t} else {\n\n\t\tnodeVar1 = ( f_span.x * exp( ( log( ( f_span.y / f_span.x ) ) * nodeVarying3.x ) ) );\n\t\tnodeVar2 = ( ( nodeVar1 / f_bolt.y ) * ( nodeVar1 / f_bolt.y ) );\n\t\tnodeVar0 = ( ( nodeVar2 / ( 1.0 + nodeVar2 ) ) * exp( ( ( - nodeVar1 ) / f_bolt.z ) ) );\n\n\t}\n\n\n\t// result\n\tfragColor = vec4( ( mix( vec3( 0.05, 0.05, 0.16 ), vec3( 1.0, 0.95, 0.7 ), ( clamp( ( ( ( f_bolt.x / f_bolt.w ) * nodeVar0 ) / f_bolt.x ), 0.0, 1.0 ) * clamp( ( 1.0 - abs( ( ( nodeVarying3.y * 2.0 ) - 1.0 ) ) ), 0.0, 1.0 ) ) ) * nodeVarying4.xyz ), nodeVarying4.w );\n\n}\n"
  },
  "transplanted": {
   "wgsl": "// transplanted into the heidler sprite shell from three's WGSL node builder by render/tslSource.mjs\nstruct Cam { viewProj: mat4x4<f32>, bolt: vec4<f32>, span: vec4<f32> };\n@group(0) @binding(0) var<uniform> cam: Cam;\nfn turned(p: vec3<f32>, yaw: f32) -> vec3<f32> {\n  let ca = cos(yaw); let sa = sin(yaw);\n  return vec3<f32>(p.x * ca - p.y * sa, p.x * sa + p.y * ca, p.z);\n}\nstruct VOut { @builtin(position) pos: vec4<f32>, @location(0) color: vec4<f32>, @location(1) uv: vec2<f32> };\n@vertex fn vs(@location(0) p: vec3<f32>, @location(1) color: vec4<f32>, @location(2) rec: vec4<f32>, @location(3) ident: vec4<f32>, @location(5) extra: vec4<f32>, @location(4) uv: vec2<f32>) -> VOut {\n  var o: VOut; var pl = p;\n  \n  o.pos = cam.viewProj * vec4<f32>(rec.xyz + turned(pl, extra.x) * rec.w, 1.0);\n  o.color = color; o.uv = uv;\n  return o;\n}\n\n@fragment fn fs(v: VOut) -> @location(0) vec4<f32> {\n\n\t// vars\n\t\n\tvar nodeVar0 : f32;\n\tvar nodeVar1 : f32;\n\tvar nodeVar2 : f32;\n\n\n\t// flow\n\t// code\n\n\n\tif ( ( ( cam.span.x * exp( ( log( ( cam.span.y / cam.span.x ) ) * v.uv.x ) ) ) <= 0.0 ) ) {\n\n\t\tnodeVar0 = 0.0;\n\n\t} else {\n\n\t\tnodeVar1 = ( cam.span.x * exp( ( log( ( cam.span.y / cam.span.x ) ) * v.uv.x ) ) );\n\t\tnodeVar2 = ( ( nodeVar1 / cam.bolt.y ) * ( nodeVar1 / cam.bolt.y ) );\n\t\tnodeVar0 = ( ( nodeVar2 / ( 1.0 + nodeVar2 ) ) * exp( ( ( - nodeVar1 ) / cam.bolt.z ) ) );\n\n\t}\n\n\n\t// result\n\n\treturn vec4<f32>( ( mix( vec3<f32>( 0.05, 0.05, 0.16 ), vec3<f32>( 1.0, 0.95, 0.7 ), ( clamp( ( ( ( cam.bolt.x / cam.bolt.w ) * nodeVar0 ) / cam.bolt.x ), 0.0, 1.0 ) * clamp( ( 1.0 - abs( ( ( v.uv.y * 2.0 ) - 1.0 ) ) ), 0.0, 1.0 ) ) ) * v.color.xyz ), v.color.w );\n\n}\n",
   "glsl": "#version 300 es\nprecision highp float;\nprecision highp sampler2D;\nuniform mat4 viewProj; uniform vec4 bolt; uniform vec4 span;\nin vec4 vColor; in vec2 vUv; out vec4 fragColor;\n\nvoid main() {\n\n\t// vars\n\tfloat nodeVar0;\n\tfloat nodeVar1;\n\tfloat nodeVar2;\n\n\t// flow\n\t// code\n\n\n\tif ( ( ( span.x * exp( ( log( ( span.y / span.x ) ) * vUv.x ) ) ) <= 0.0 ) ) {\n\n\t\tnodeVar0 = 0.0;\n\n\t} else {\n\n\t\tnodeVar1 = ( span.x * exp( ( log( ( span.y / span.x ) ) * vUv.x ) ) );\n\t\tnodeVar2 = ( ( nodeVar1 / bolt.y ) * ( nodeVar1 / bolt.y ) );\n\t\tnodeVar0 = ( ( nodeVar2 / ( 1.0 + nodeVar2 ) ) * exp( ( ( - nodeVar1 ) / bolt.z ) ) );\n\n\t}\n\n\n\t// result\n\tfragColor = vec4( ( mix( vec3( 0.05, 0.05, 0.16 ), vec3( 1.0, 0.95, 0.7 ), ( clamp( ( ( ( bolt.x / bolt.w ) * nodeVar0 ) / bolt.x ), 0.0, 1.0 ) * clamp( ( 1.0 - abs( ( ( vUv.y * 2.0 ) - 1.0 ) ) ), 0.0, 1.0 ) ) ) * vColor.xyz ), vColor.w );\n\n}\n"
  }
 },
 "atlas": {
  "note": "v4326 -- the fleets' own sprite look as three's builders emitted it, and as tslSource transplanted it into the sprite shell with the atlas bound",
  "wgsl": {
   "language": "wgsl",
   "vertex": "// Three.js r178 - Node System\n\n// directives\n\n\n// structs\n\n\n// uniforms\n\nstruct renderStruct {\n\tcameraProjectionMatrix : mat4x4<f32>,\n\tcameraViewMatrix : mat4x4<f32>\n};\n@binding( 0 ) @group( 0 )\nvar<uniform> render : renderStruct;\n\nstruct objectStruct {\n\tnodeUniform3 : mat4x4<f32>\n};\n@binding( 1 ) @group( 1 )\nvar<uniform> object : objectStruct;\n\n// varyings\n\nstruct VaryingsStruct {\n\t@location( 3 ) nodeVarying3 : vec2<f32>,\n\t@location( 4 ) nodeVarying4 : vec4<f32>,\n\t@builtin( position ) Vertex : vec4<f32>\n};\nvar<private> varyings : VaryingsStruct;\n\n// codes\n\n\n@vertex\nfn main( @location( 0 ) uv : vec2<f32>,\n\t@location( 1 ) color : vec4<f32>,\n\t@location( 2 ) position : vec3<f32> ) -> VaryingsStruct {\n\n\t// vars\n\t\n\tvar modelViewMatrix : mat4x4<f32>;\n\tvar nodeVar2 : vec4<f32>;\n\tvar v_modelViewProjection : vec4<f32>;\n\tvar v_positionView : vec3<f32>;\n\tvar positionLocal : vec3<f32>;\n\n\n\t// flow\n\t// code\n\n\tvaryings.nodeVarying3 = uv;\n\tvaryings.nodeVarying4 = color;\n\tpositionLocal = position;\n\tmodelViewMatrix = ( render.cameraViewMatrix * object.nodeUniform3 );\n\tv_positionView = ( modelViewMatrix * vec4<f32>( positionLocal, 1.0 ) ).xyz;\n\tnodeVar2 = ( render.cameraProjectionMatrix * vec4<f32>( v_positionView, 1.0 ) );\n\tv_modelViewProjection = nodeVar2;\n\n\t// result\n\n\tvaryings.Vertex = v_modelViewProjection;\n\n\treturn varyings;\n\n}\n",
   "fragment": "// Three.js r178 - Node System\n\n// global\ndiagnostic( off, derivative_uniformity );\n\n\n// structs\n\nstruct OutputStruct {\n\t@location(0) color: vec4<f32>\n};\nvar<private> output : OutputStruct;\n\n// uniforms\n@binding( 0 ) @group( 1 ) var atlas : texture_2d<f32>;\n\n// codes\n\n\n@fragment\nfn main( @location( 3 ) nodeVarying3 : vec2<f32>,\n\t@location( 4 ) nodeVarying4 : vec4<f32> ) -> OutputStruct {\n\n\t// vars\n\t\n\tvar nodeVar0 : vec4<f32>;\n\tvar nodeVar1 : vec4<f32>;\n\n\n\t// flow\n\t// code\n\n\tnodeVar0 = textureLoad( atlas, vec2<i32>( clamp( ( nodeVarying3 * vec2<f32>( textureDimensions( atlas, 0 ) ) ), vec2<f32>( 0.0, 0.0 ), ( vec2<f32>( textureDimensions( atlas, 0 ) ) - vec2<f32>( 1.0 ) ) ) ), u32( 0u ) );\n\n\tif ( ( nodeVar0.w < 0.5 ) ) {\n\n\t\tdiscard;\n\t\t\n\n\t}\n\n\tnodeVar1 = textureLoad( atlas, vec2<i32>( clamp( ( nodeVarying3 * vec2<f32>( textureDimensions( atlas, 0 ) ) ), vec2<f32>( 0.0, 0.0 ), ( vec2<f32>( textureDimensions( atlas, 0 ) ) - vec2<f32>( 1.0 ) ) ) ), u32( 0u ) );\n\n\t// result\n\n\toutput.color = vec4<f32>( ( nodeVar1.xyz * nodeVarying4.xyz ), 1.0 );\n\n\treturn output;\n\n}\n"
  },
  "glsl": {
   "language": "glsl",
   "vertex": "#version 300 es\n\n// Three.js r178 - Node System\n\n\n// extensions\n\n\n// precision\n\nprecision highp float;\nprecision highp int;\nprecision highp sampler2D;\nprecision highp sampler3D;\nprecision highp samplerCube;\nprecision highp sampler2DArray;\n\nprecision highp usampler2D;\nprecision highp usampler3D;\nprecision highp usamplerCube;\nprecision highp usampler2DArray;\n\nprecision highp isampler2D;\nprecision highp isampler3D;\nprecision highp isamplerCube;\nprecision highp isampler2DArray;\n\nprecision lowp sampler2DShadow;\nprecision lowp sampler2DArrayShadow;\nprecision lowp samplerCubeShadow;\n\n\n// uniforms\n\nlayout( std140 ) uniform vertex_render {\n\tmat4 v_cameraProjectionMatrix;\n\tmat4 v_cameraViewMatrix;\n};\n\nlayout( std140 ) uniform vertex_object {\n\tmat4 v_nodeUniform3;\n};\n\n\n// varyings\nvec4 v_modelViewProjection;\nvec3 v_positionView;\nvec3 positionLocal;\nout vec2 nodeVarying3;\nout vec4 nodeVarying4;\n\n\n// attributes\nlayout( location = 0 ) in vec2 uv;\nlayout( location = 1 ) in vec4 color;\nlayout( location = 2 ) in vec3 position;\n\n\n// codes\n\n\nvoid main() {\n\n\t// vars\n\tmat4 modelViewMatrix;\n\tvec4 nodeVar2;\n\n\t// transforms\n\t\n\n\t// flow\n\t// code\n\n\tnodeVarying3 = uv;\n\tnodeVarying4 = color;\n\tpositionLocal = position;\n\tmodelViewMatrix = ( v_cameraViewMatrix * v_nodeUniform3 );\n\tv_positionView = ( modelViewMatrix * vec4( positionLocal, 1.0 ) ).xyz;\n\tnodeVar2 = ( v_cameraProjectionMatrix * vec4( v_positionView, 1.0 ) );\n\tv_modelViewProjection = nodeVar2;\n\n\t// result\n\tgl_Position = v_modelViewProjection;\n\n\tgl_PointSize = 1.0;\n\n}\n",
   "fragment": "#version 300 es\n\n// Three.js r178 - Node System\n\n\n// extensions\n\n\n// precision\n\nprecision highp float;\nprecision highp int;\nprecision highp sampler2D;\nprecision highp sampler3D;\nprecision highp samplerCube;\nprecision highp sampler2DArray;\n\nprecision highp usampler2D;\nprecision highp usampler3D;\nprecision highp usamplerCube;\nprecision highp usampler2DArray;\n\nprecision highp isampler2D;\nprecision highp isampler3D;\nprecision highp isamplerCube;\nprecision highp isampler2DArray;\n\nprecision lowp sampler2DShadow;\nprecision lowp sampler2DArrayShadow;\nprecision lowp samplerCubeShadow;\n\n\n// uniforms\nuniform sampler2D atlas;\n\n// varyings\nin vec2 nodeVarying3;\nin vec4 nodeVarying4;\n\n\n// codes\n\n\n// structs\n\nlayout( location = 0 ) out vec4 fragColor;\n\n\n\nvoid main() {\n\n\t// vars\n\tvec4 nodeVar0;\n\tvec4 nodeVar1;\n\n\t// flow\n\t// code\n\n\tnodeVar0 = texelFetch( atlas, ivec2( clamp( ( nodeVarying3 * vec2( textureSize( atlas, 0 ) ) ), vec2( 0.0, 0.0 ), ( vec2( textureSize( atlas, 0 ) ) - vec2( 1.0 ) ) ) ), 0 );\n\n\tif ( ( nodeVar0.w < 0.5 ) ) {\n\n\t\tdiscard;\n\t\t\n\n\t}\n\n\tnodeVar1 = texelFetch( atlas, ivec2( clamp( ( nodeVarying3 * vec2( textureSize( atlas, 0 ) ) ), vec2( 0.0, 0.0 ), ( vec2( textureSize( atlas, 0 ) ) - vec2( 1.0 ) ) ) ), 0 );\n\n\t// result\n\tfragColor = vec4( ( nodeVar1.xyz * nodeVarying4.xyz ), 1.0 );\n\n}\n"
  },
  "transplanted": {
   "wgsl": "// transplanted into the sprite (atlas) shell from three's WGSL node builder by render/tslSource.mjs\nstruct Cam { viewProj: mat4x4<f32> };\n@group(0) @binding(0) var<uniform> cam: Cam;\n@group(0) @binding(1) var atlas: texture_2d<f32>;\nfn turned(p: vec3<f32>, yaw: f32) -> vec3<f32> {\n  let ca = cos(yaw); let sa = sin(yaw);\n  return vec3<f32>(p.x * ca - p.y * sa, p.x * sa + p.y * ca, p.z);\n}\nstruct VOut { @builtin(position) pos: vec4<f32>, @location(0) color: vec4<f32>, @location(1) uv: vec2<f32> };\n@vertex fn vs(@location(0) p: vec3<f32>, @location(1) color: vec4<f32>, @location(2) rec: vec4<f32>, @location(3) ident: vec4<f32>, @location(5) extra: vec4<f32>, @location(4) uv: vec2<f32>) -> VOut {\n  var o: VOut; var pl = p;\n  \n  o.pos = cam.viewProj * vec4<f32>(rec.xyz + turned(pl, extra.x) * rec.w, 1.0);\n  o.color = color; o.uv = uv;\n  return o;\n}\n\n@fragment fn fs(v: VOut) -> @location(0) vec4<f32> {\n\n\t// vars\n\t\n\tvar nodeVar0 : vec4<f32>;\n\tvar nodeVar1 : vec4<f32>;\n\n\n\t// flow\n\t// code\n\n\tnodeVar0 = textureLoad( atlas, vec2<i32>( clamp( ( v.uv * vec2<f32>( textureDimensions( atlas, 0 ) ) ), vec2<f32>( 0.0, 0.0 ), ( vec2<f32>( textureDimensions( atlas, 0 ) ) - vec2<f32>( 1.0 ) ) ) ), u32( 0u ) );\n\n\tif ( ( nodeVar0.w < 0.5 ) ) {\n\n\t\tdiscard;\n\t\t\n\n\t}\n\n\tnodeVar1 = textureLoad( atlas, vec2<i32>( clamp( ( v.uv * vec2<f32>( textureDimensions( atlas, 0 ) ) ), vec2<f32>( 0.0, 0.0 ), ( vec2<f32>( textureDimensions( atlas, 0 ) ) - vec2<f32>( 1.0 ) ) ) ), u32( 0u ) );\n\n\t// result\n\n\treturn vec4<f32>( ( nodeVar1.xyz * v.color.xyz ), 1.0 );\n\n}\n",
   "glsl": "#version 300 es\nprecision highp float;\nprecision highp sampler2D;\nuniform mat4 viewProj; uniform sampler2D atlas;\nin vec4 vColor; in vec2 vUv; out vec4 fragColor;\n\nvoid main() {\n\n\t// vars\n\tvec4 nodeVar0;\n\tvec4 nodeVar1;\n\n\t// flow\n\t// code\n\n\tnodeVar0 = texelFetch( atlas, ivec2( clamp( ( vUv * vec2( textureSize( atlas, 0 ) ) ), vec2( 0.0, 0.0 ), ( vec2( textureSize( atlas, 0 ) ) - vec2( 1.0 ) ) ) ), 0 );\n\n\tif ( ( nodeVar0.w < 0.5 ) ) {\n\n\t\tdiscard;\n\t\t\n\n\t}\n\n\tnodeVar1 = texelFetch( atlas, ivec2( clamp( ( vUv * vec2( textureSize( atlas, 0 ) ) ), vec2( 0.0, 0.0 ), ( vec2( textureSize( atlas, 0 ) ) - vec2( 1.0 ) ) ) ), 0 );\n\n\t// result\n\tfragColor = vec4( ( nodeVar1.xyz * vColor.xyz ), 1.0 );\n\n}\n"
  }
 },
 "sampled": {
  "note": "v4327 -- the filtered sprite look as three emitted it, and as tslSource transplanted it into the sprite shell with the atlas and a sampler",
  "wgsl": {
   "language": "wgsl",
   "vertex": "// Three.js r178 - Node System\n\n// directives\n\n\n// structs\n\n\n// uniforms\n\nstruct renderStruct {\n\tcameraProjectionMatrix : mat4x4<f32>,\n\tcameraViewMatrix : mat4x4<f32>\n};\n@binding( 0 ) @group( 0 )\nvar<uniform> render : renderStruct;\n\nstruct objectStruct {\n\tnodeUniform3 : mat4x4<f32>\n};\n@binding( 2 ) @group( 1 )\nvar<uniform> object : objectStruct;\n\n// varyings\n\nstruct VaryingsStruct {\n\t@location( 3 ) nodeVarying3 : vec2<f32>,\n\t@location( 4 ) nodeVarying4 : vec4<f32>,\n\t@builtin( position ) Vertex : vec4<f32>\n};\nvar<private> varyings : VaryingsStruct;\n\n// codes\n\n\n@vertex\nfn main( @location( 0 ) uv : vec2<f32>,\n\t@location( 1 ) color : vec4<f32>,\n\t@location( 2 ) position : vec3<f32> ) -> VaryingsStruct {\n\n\t// vars\n\t\n\tvar modelViewMatrix : mat4x4<f32>;\n\tvar nodeVar2 : vec4<f32>;\n\tvar v_modelViewProjection : vec4<f32>;\n\tvar v_positionView : vec3<f32>;\n\tvar positionLocal : vec3<f32>;\n\n\n\t// flow\n\t// code\n\n\tvaryings.nodeVarying3 = uv;\n\tvaryings.nodeVarying4 = color;\n\tpositionLocal = position;\n\tmodelViewMatrix = ( render.cameraViewMatrix * object.nodeUniform3 );\n\tv_positionView = ( modelViewMatrix * vec4<f32>( positionLocal, 1.0 ) ).xyz;\n\tnodeVar2 = ( render.cameraProjectionMatrix * vec4<f32>( v_positionView, 1.0 ) );\n\tv_modelViewProjection = nodeVar2;\n\n\t// result\n\n\tvaryings.Vertex = v_modelViewProjection;\n\n\treturn varyings;\n\n}\n",
   "fragment": "// Three.js r178 - Node System\n\n// global\ndiagnostic( off, derivative_uniformity );\n\n\n// structs\n\nstruct OutputStruct {\n\t@location(0) color: vec4<f32>\n};\nvar<private> output : OutputStruct;\n\n// uniforms\n@binding( 0 ) @group( 1 ) var atlas_sampler : sampler;\n@binding( 1 ) @group( 1 ) var atlas : texture_2d<f32>;\n\n// codes\n\n\n@fragment\nfn main( @location( 3 ) nodeVarying3 : vec2<f32>,\n\t@location( 4 ) nodeVarying4 : vec4<f32> ) -> OutputStruct {\n\n\t// vars\n\t\n\tvar nodeVar0 : vec4<f32>;\n\tvar nodeVar1 : vec4<f32>;\n\n\n\t// flow\n\t// code\n\n\tnodeVar0 = textureSample( atlas, atlas_sampler, nodeVarying3 );\n\n\tif ( ( nodeVar0.w < 0.5 ) ) {\n\n\t\tdiscard;\n\t\t\n\n\t}\n\n\tnodeVar1 = textureSample( atlas, atlas_sampler, nodeVarying3 );\n\n\t// result\n\n\toutput.color = vec4<f32>( ( nodeVar1.xyz * nodeVarying4.xyz ), 1.0 );\n\n\treturn output;\n\n}\n"
  },
  "glsl": {
   "language": "glsl",
   "vertex": "#version 300 es\n\n// Three.js r178 - Node System\n\n\n// extensions\n\n\n// precision\n\nprecision highp float;\nprecision highp int;\nprecision highp sampler2D;\nprecision highp sampler3D;\nprecision highp samplerCube;\nprecision highp sampler2DArray;\n\nprecision highp usampler2D;\nprecision highp usampler3D;\nprecision highp usamplerCube;\nprecision highp usampler2DArray;\n\nprecision highp isampler2D;\nprecision highp isampler3D;\nprecision highp isamplerCube;\nprecision highp isampler2DArray;\n\nprecision lowp sampler2DShadow;\nprecision lowp sampler2DArrayShadow;\nprecision lowp samplerCubeShadow;\n\n\n// uniforms\n\nlayout( std140 ) uniform vertex_render {\n\tmat4 v_cameraProjectionMatrix;\n\tmat4 v_cameraViewMatrix;\n};\n\nlayout( std140 ) uniform vertex_object {\n\tmat4 v_nodeUniform3;\n};\n\n\n// varyings\nvec4 v_modelViewProjection;\nvec3 v_positionView;\nvec3 positionLocal;\nout vec2 nodeVarying3;\nout vec4 nodeVarying4;\n\n\n// attributes\nlayout( location = 0 ) in vec2 uv;\nlayout( location = 1 ) in vec4 color;\nlayout( location = 2 ) in vec3 position;\n\n\n// codes\n\n\nvoid main() {\n\n\t// vars\n\tmat4 modelViewMatrix;\n\tvec4 nodeVar2;\n\n\t// transforms\n\t\n\n\t// flow\n\t// code\n\n\tnodeVarying3 = uv;\n\tnodeVarying4 = color;\n\tpositionLocal = position;\n\tmodelViewMatrix = ( v_cameraViewMatrix * v_nodeUniform3 );\n\tv_positionView = ( modelViewMatrix * vec4( positionLocal, 1.0 ) ).xyz;\n\tnodeVar2 = ( v_cameraProjectionMatrix * vec4( v_positionView, 1.0 ) );\n\tv_modelViewProjection = nodeVar2;\n\n\t// result\n\tgl_Position = v_modelViewProjection;\n\n\tgl_PointSize = 1.0;\n\n}\n",
   "fragment": "#version 300 es\n\n// Three.js r178 - Node System\n\n\n// extensions\n\n\n// precision\n\nprecision highp float;\nprecision highp int;\nprecision highp sampler2D;\nprecision highp sampler3D;\nprecision highp samplerCube;\nprecision highp sampler2DArray;\n\nprecision highp usampler2D;\nprecision highp usampler3D;\nprecision highp usamplerCube;\nprecision highp usampler2DArray;\n\nprecision highp isampler2D;\nprecision highp isampler3D;\nprecision highp isamplerCube;\nprecision highp isampler2DArray;\n\nprecision lowp sampler2DShadow;\nprecision lowp sampler2DArrayShadow;\nprecision lowp samplerCubeShadow;\n\n\n// uniforms\nuniform sampler2D atlas;\n\n// varyings\nin vec2 nodeVarying3;\nin vec4 nodeVarying4;\n\n\n// codes\n\n\n// structs\n\nlayout( location = 0 ) out vec4 fragColor;\n\n\n\nvoid main() {\n\n\t// vars\n\tvec4 nodeVar0;\n\tvec4 nodeVar1;\n\n\t// flow\n\t// code\n\n\tnodeVar0 = texture( atlas, nodeVarying3 );\n\n\tif ( ( nodeVar0.w < 0.5 ) ) {\n\n\t\tdiscard;\n\t\t\n\n\t}\n\n\tnodeVar1 = texture( atlas, nodeVarying3 );\n\n\t// result\n\tfragColor = vec4( ( nodeVar1.xyz * nodeVarying4.xyz ), 1.0 );\n\n}\n"
  },
  "transplanted": {
   "wgsl": "// transplanted into the sprite (atlas + sampler) shell from three's WGSL node builder by render/tslSource.mjs\nstruct Cam { viewProj: mat4x4<f32> };\n@group(0) @binding(0) var<uniform> cam: Cam;\n@group(0) @binding(1) var atlas: texture_2d<f32>;\n@group(0) @binding(2) var samp: sampler;\nfn turned(p: vec3<f32>, yaw: f32) -> vec3<f32> {\n  let ca = cos(yaw); let sa = sin(yaw);\n  return vec3<f32>(p.x * ca - p.y * sa, p.x * sa + p.y * ca, p.z);\n}\nstruct VOut { @builtin(position) pos: vec4<f32>, @location(0) color: vec4<f32>, @location(1) uv: vec2<f32> };\n@vertex fn vs(@location(0) p: vec3<f32>, @location(1) color: vec4<f32>, @location(2) rec: vec4<f32>, @location(3) ident: vec4<f32>, @location(5) extra: vec4<f32>, @location(4) uv: vec2<f32>) -> VOut {\n  var o: VOut; var pl = p;\n  \n  o.pos = cam.viewProj * vec4<f32>(rec.xyz + turned(pl, extra.x) * rec.w, 1.0);\n  o.color = color; o.uv = uv;\n  return o;\n}\n\n@fragment fn fs(v: VOut) -> @location(0) vec4<f32> {\n\n\t// vars\n\t\n\tvar nodeVar0 : vec4<f32>;\n\tvar nodeVar1 : vec4<f32>;\n\n\n\t// flow\n\t// code\n\n\tnodeVar0 = textureSample( atlas, samp, v.uv );\n\n\tif ( ( nodeVar0.w < 0.5 ) ) {\n\n\t\tdiscard;\n\t\t\n\n\t}\n\n\tnodeVar1 = textureSample( atlas, samp, v.uv );\n\n\t// result\n\n\treturn vec4<f32>( ( nodeVar1.xyz * v.color.xyz ), 1.0 );\n\n}\n",
   "glsl": "#version 300 es\nprecision highp float;\nprecision highp sampler2D;\nuniform mat4 viewProj; uniform sampler2D atlas;\nin vec4 vColor; in vec2 vUv; out vec4 fragColor;\n\nvoid main() {\n\n\t// vars\n\tvec4 nodeVar0;\n\tvec4 nodeVar1;\n\n\t// flow\n\t// code\n\n\tnodeVar0 = texture( atlas, vUv );\n\n\tif ( ( nodeVar0.w < 0.5 ) ) {\n\n\t\tdiscard;\n\t\t\n\n\t}\n\n\tnodeVar1 = texture( atlas, vUv );\n\n\t// result\n\tfragColor = vec4( ( nodeVar1.xyz * vColor.xyz ), 1.0 );\n\n}\n"
  }
 },
 "ink": {
  "note": "v4328 -- the ink wash as three's builders emitted it, and as tslSource transplanted it into the fleets' line-list shell",
  "wgsl": {
   "language": "wgsl",
   "vertex": "// Three.js r178 - Node System\n\n// directives\n\n\n// structs\n\n\n// uniforms\n\nstruct renderStruct {\n\tcameraProjectionMatrix : mat4x4<f32>,\n\tcameraViewMatrix : mat4x4<f32>\n};\n@binding( 0 ) @group( 0 )\nvar<uniform> render : renderStruct;\n\nstruct objectStruct {\n\tnodeUniform3 : mat4x4<f32>\n};\n@binding( 1 ) @group( 1 )\nvar<uniform> object : objectStruct;\n\n// varyings\n\nstruct VaryingsStruct {\n\t@location( 3 ) nodeVarying3 : vec4<f32>,\n\t@builtin( position ) Vertex : vec4<f32>\n};\nvar<private> varyings : VaryingsStruct;\n\n// codes\n\n\n@vertex\nfn main( @location( 0 ) color : vec4<f32>,\n\t@location( 1 ) position : vec3<f32> ) -> VaryingsStruct {\n\n\t// vars\n\t\n\tvar modelViewMatrix : mat4x4<f32>;\n\tvar nodeVar0 : vec4<f32>;\n\tvar v_modelViewProjection : vec4<f32>;\n\tvar v_positionView : vec3<f32>;\n\tvar positionLocal : vec3<f32>;\n\n\n\t// flow\n\t// code\n\n\tvaryings.nodeVarying3 = color;\n\tpositionLocal = position;\n\tmodelViewMatrix = ( render.cameraViewMatrix * object.nodeUniform3 );\n\tv_positionView = ( modelViewMatrix * vec4<f32>( positionLocal, 1.0 ) ).xyz;\n\tnodeVar0 = ( render.cameraProjectionMatrix * vec4<f32>( v_positionView, 1.0 ) );\n\tv_modelViewProjection = nodeVar0;\n\n\t// result\n\n\tvaryings.Vertex = v_modelViewProjection;\n\n\treturn varyings;\n\n}\n",
   "fragment": "// Three.js r178 - Node System\n\n// global\ndiagnostic( off, derivative_uniformity );\n\n\n// structs\n\nstruct OutputStruct {\n\t@location(0) color: vec4<f32>\n};\nvar<private> output : OutputStruct;\n\n// uniforms\n\nstruct objectStruct {\n\tink : vec4<f32>\n};\n@binding( 0 ) @group( 1 )\nvar<uniform> object : objectStruct;\n\n// codes\n\n\n@fragment\nfn main( @location( 3 ) nodeVarying3 : vec4<f32> ) -> OutputStruct {\n\n\t// vars\n\t\n\n\n\n\t// flow\n\t// code\n\n\n\t// result\n\n\toutput.color = vec4<f32>( ( mix( nodeVarying3.xyz, vec3<f32>( dot( nodeVarying3.xyz, vec3<f32>( 0.299, 0.587, 0.114 ) ) ), object.ink.x ) * vec3<f32>( object.ink.y ) ), nodeVarying3.w );\n\n\treturn output;\n\n}\n"
  },
  "glsl": {
   "language": "glsl",
   "vertex": "#version 300 es\n\n// Three.js r178 - Node System\n\n\n// extensions\n\n\n// precision\n\nprecision highp float;\nprecision highp int;\nprecision highp sampler2D;\nprecision highp sampler3D;\nprecision highp samplerCube;\nprecision highp sampler2DArray;\n\nprecision highp usampler2D;\nprecision highp usampler3D;\nprecision highp usamplerCube;\nprecision highp usampler2DArray;\n\nprecision highp isampler2D;\nprecision highp isampler3D;\nprecision highp isamplerCube;\nprecision highp isampler2DArray;\n\nprecision lowp sampler2DShadow;\nprecision lowp sampler2DArrayShadow;\nprecision lowp samplerCubeShadow;\n\n\n// uniforms\n\nlayout( std140 ) uniform vertex_render {\n\tmat4 v_cameraProjectionMatrix;\n\tmat4 v_cameraViewMatrix;\n};\n\nlayout( std140 ) uniform vertex_object {\n\tmat4 v_nodeUniform3;\n};\n\n\n// varyings\nvec4 v_modelViewProjection;\nvec3 v_positionView;\nvec3 positionLocal;\nout vec4 nodeVarying3;\n\n\n// attributes\nlayout( location = 0 ) in vec4 color;\nlayout( location = 1 ) in vec3 position;\n\n\n// codes\n\n\nvoid main() {\n\n\t// vars\n\tmat4 modelViewMatrix;\n\tvec4 nodeVar0;\n\n\t// transforms\n\t\n\n\t// flow\n\t// code\n\n\tnodeVarying3 = color;\n\tpositionLocal = position;\n\tmodelViewMatrix = ( v_cameraViewMatrix * v_nodeUniform3 );\n\tv_positionView = ( modelViewMatrix * vec4( positionLocal, 1.0 ) ).xyz;\n\tnodeVar0 = ( v_cameraProjectionMatrix * vec4( v_positionView, 1.0 ) );\n\tv_modelViewProjection = nodeVar0;\n\n\t// result\n\tgl_Position = v_modelViewProjection;\n\n\tgl_PointSize = 1.0;\n\n}\n",
   "fragment": "#version 300 es\n\n// Three.js r178 - Node System\n\n\n// extensions\n\n\n// precision\n\nprecision highp float;\nprecision highp int;\nprecision highp sampler2D;\nprecision highp sampler3D;\nprecision highp samplerCube;\nprecision highp sampler2DArray;\n\nprecision highp usampler2D;\nprecision highp usampler3D;\nprecision highp usamplerCube;\nprecision highp usampler2DArray;\n\nprecision highp isampler2D;\nprecision highp isampler3D;\nprecision highp isamplerCube;\nprecision highp isampler2DArray;\n\nprecision lowp sampler2DShadow;\nprecision lowp sampler2DArrayShadow;\nprecision lowp samplerCubeShadow;\n\n\n// uniforms\n\nlayout( std140 ) uniform fragment_object {\n\tvec4 f_ink;\n};\n\n\n// varyings\nin vec4 nodeVarying3;\n\n\n// codes\n\n\n// structs\n\nlayout( location = 0 ) out vec4 fragColor;\n\n\n\nvoid main() {\n\n\t// vars\n\t\n\n\t// flow\n\t// code\n\n\n\t// result\n\tfragColor = vec4( ( mix( nodeVarying3.xyz, vec3( dot( nodeVarying3.xyz, vec3( 0.299, 0.587, 0.114 ) ) ), f_ink.x ) * vec3( f_ink.y ) ), nodeVarying3.w );\n\n}\n"
  },
  "transplanted": {
   "wgsl": "// transplanted into the ink shell from three's WGSL node builder by render/tslSource.mjs\nstruct Cam { viewProj: mat4x4<f32>, ink: vec4<f32> };\n@group(0) @binding(0) var<uniform> cam: Cam;\nfn turned(p: vec3<f32>, yaw: f32) -> vec3<f32> {\n  let ca = cos(yaw); let sa = sin(yaw);\n  return vec3<f32>(p.x * ca - p.y * sa, p.x * sa + p.y * ca, p.z);\n}\nstruct VOut { @builtin(position) pos: vec4<f32>, @location(0) color: vec4<f32> };\n@vertex fn vs(@location(0) p: vec3<f32>, @location(1) color: vec4<f32>, @location(2) rec: vec4<f32>, @location(3) ident: vec4<f32>, @location(5) extra: vec4<f32>) -> VOut {\n  var o: VOut; var pl = p;\n  \n  o.pos = cam.viewProj * vec4<f32>(rec.xyz + turned(pl, extra.x) * rec.w, 1.0);\n  o.color = color;\n  return o;\n}\n\n@fragment fn fs(v: VOut) -> @location(0) vec4<f32> {\n\n\t// vars\n\t\n\n\n\n\t// flow\n\t// code\n\n\n\t// result\n\n\treturn vec4<f32>( ( mix( v.color.xyz, vec3<f32>( dot( v.color.xyz, vec3<f32>( 0.299, 0.587, 0.114 ) ) ), cam.ink.x ) * vec3<f32>( cam.ink.y ) ), v.color.w );\n\n}\n",
   "glsl": "#version 300 es\nprecision highp float;\nuniform mat4 viewProj; uniform vec4 ink;\nin vec4 vColor; out vec4 fragColor;\n\nvoid main() {\n\n\t// vars\n\t\n\n\t// flow\n\t// code\n\n\n\t// result\n\tfragColor = vec4( ( mix( vColor.xyz, vec3( dot( vColor.xyz, vec3( 0.299, 0.587, 0.114 ) ) ), ink.x ) * vec3( ink.y ) ), vColor.w );\n\n}\n"
  }
 }
}
{
 "at": "v4322",
 "three": "0.178.0",
 "note": "the Lyapunov look as three's node builders emitted it from render/physicsTsl.mjs makeLyapunovLookTsl, and as render/tslSource.mjs transplanted it into the look's own shell; rewritten by tools/ship/tslRace-selfcheck.mjs on every run",
 "wgsl": {
  "language": "wgsl",
  "vertex": "// Three.js r185 - Node System\n\n// directives\n\n\n// structs\n\n\n// uniforms\n\nstruct renderStruct {\n\tcameraProjectionMatrix : mat4x4<f32>,\n\tcameraViewMatrix : mat4x4<f32>\n};\n@binding( 0 ) @group( 0 )\nvar<uniform> render : renderStruct;\n\nstruct objectStruct {\n\tchaos : vec4<f32>,\n\tlight : vec4<f32>,\n\tnodeUniform4 : mat4x4<f32>\n};\n@binding( 0 ) @group( 1 )\nvar<uniform> object : objectStruct;\n\n// varyings\n\nstruct VaryingsStruct {\n\t@location( 0 ) nodeVarying4 : vec2<f32>,\n\t@location( 1 ) nodeVarying5 : vec3<f32>,\n\t@location( 2 ) nodeVarying6 : vec4<f32>,\n\t@builtin( position ) builtinClipSpace : vec4<f32>\n};\nvar<private> varyings : VaryingsStruct;\n\n// vars\nvar<private> modelViewMatrix : mat4x4<f32>;\nvar<private> VERTEX_nodeVar3 : vec4<f32>;\nvar<private> v_modelViewProjection : vec4<f32>;\nvar<private> v_positionView : vec3<f32>;\nvar<private> positionLocal : vec3<f32>;\nvar<private> VERTEX_v_modelViewProjection : vec4<f32>;\n\n// codes\n\n\n@vertex\nfn main( @location( 0 ) uv : vec2<f32>,\n\t@location( 1 ) normal : vec3<f32>,\n\t@location( 2 ) color : vec4<f32>,\n\t@location( 3 ) position : vec3<f32> ) -> VaryingsStruct {\n\n\t// flow\n\t// code\n\n\tvaryings.nodeVarying4 = uv;\n\tvaryings.nodeVarying5 = normal;\n\tvaryings.nodeVarying6 = color;\n\tmodelViewMatrix = ( render.cameraViewMatrix * object.nodeUniform4 );\n\tpositionLocal = position;\n\tv_positionView = ( modelViewMatrix * vec4<f32>( positionLocal, 1.0 ) ).xyz;\n\tVERTEX_nodeVar3 = ( render.cameraProjectionMatrix * vec4<f32>( v_positionView, 1.0 ) );\n\tVERTEX_v_modelViewProjection = VERTEX_nodeVar3;\n\n\t// result\n\n\tvaryings.builtinClipSpace = VERTEX_v_modelViewProjection;\n\n\treturn varyings;\n\n}\n",
  "fragment": "// Three.js r185 - Node System\n\n// global\ndiagnostic( off, derivative_uniformity );\n\n\n// structs\n\nstruct OutputStruct {\n\t@location( 0 ) color: vec4<f32>\n};\nvar<private> output : OutputStruct;\n\n// uniforms\n\nstruct objectStruct {\n\tchaos : vec4<f32>,\n\tlight : vec4<f32>,\n\tnodeUniform4 : mat4x4<f32>\n};\n@binding( 0 ) @group( 1 )\nvar<uniform> object : objectStruct;\n\n// vars\nvar<private> nodeVar0 : f32;\nvar<private> nodeVar1 : f32;\nvar<private> nodeVar2 : f32;\nvar<private> normalLocal : vec3<f32>;\n\n// codes\n\n\n@fragment\nfn main( @location( 0 ) nodeVarying4 : vec2<f32>,\n\t@location( 1 ) nodeVarying5 : vec3<f32>,\n\t@location( 2 ) nodeVarying6 : vec4<f32> ) -> OutputStruct {\n\n\t// flow\n\t// code\n\n\tnodeVar0 = ( 0.05 + ( clamp( ( ( nodeVarying4.y * 0.5 ) + 0.5 ), 0.0, 1.0 ) * 0.9 ) );\n\n\tfor ( var i : i32 = 0; i < 32; i ++ ) {\n\n\t\tnodeVar1 = ( object.chaos.x + ( ( object.chaos.y - object.chaos.x ) * clamp( ( ( nodeVarying4.x * 0.5 ) + 0.5 ), 0.0, 1.0 ) ) );\n\t\tnodeVar0 = ( ( nodeVar1 * nodeVar0 ) * ( 1.0 - nodeVar0 ) );\n\n\t}\n\n\tnodeVar2 = 0.0;\n\n\tfor ( var i : i32 = 0; i < 96; i ++ ) {\n\n\t\tnodeVar2 = ( nodeVar2 + log( abs( ( nodeVar1 * ( 1.0 - ( nodeVar0 * 2.0 ) ) ) ) ) );\n\t\tnodeVar0 = ( ( nodeVar1 * nodeVar0 ) * ( 1.0 - nodeVar0 ) );\n\n\t}\n\n\tnormalLocal = nodeVarying5;\n\n\t// result\n\n\toutput.color = vec4<f32>( ( ( mix( vec3<f32>( 0.08, 0.06, 0.2 ), vec3<f32>( 0.35, 0.95, 0.85 ), max( clamp( ( ( nodeVar2 / 96.0 ) / 0.6931471805599453 ), -1.0, 1.0 ), 0.0 ) ) * vec3<f32>( ( object.light.w + ( ( 1.0 - object.light.w ) * max( dot( normalize( normalLocal ), normalize( object.light.xyz ) ), 0.0 ) ) ) ) ) * nodeVarying6.xyz ), nodeVarying6.w );\n\n\treturn output;\n\n}\n"
 },
 "glsl": {
  "language": "glsl",
  "vertex": "#version 300 es\n\n// Three.js r185 - Node System\n\n\n// extensions\n\n\n// precision\n\nprecision highp float;\nprecision highp int;\nprecision highp sampler2D;\nprecision highp sampler3D;\nprecision highp samplerCube;\nprecision highp sampler2DArray;\n\nprecision highp usampler2D;\nprecision highp usampler3D;\nprecision highp usamplerCube;\nprecision highp usampler2DArray;\n\nprecision highp isampler2D;\nprecision highp isampler3D;\nprecision highp isamplerCube;\nprecision highp isampler2DArray;\n\nprecision highp sampler2DShadow;\nprecision highp sampler2DArrayShadow;\nprecision highp samplerCubeShadow;\n\n\n// structs\n\n\n\n\n\n// uniforms\n\nlayout( std140 ) uniform render {\n\tmat4 cameraProjectionMatrix;\n\tmat4 cameraViewMatrix;\n};\n\nlayout( std140 ) uniform object {\n\tvec4 chaos;\n\tvec4 light;\n\tmat4 nodeUniform4;\n};\n\n\n// varyings\nvec4 v_modelViewProjection;\nvec3 v_positionView;\nvec3 positionLocal;\nvec4 VERTEX_v_modelViewProjection;\nout vec2 nodeVarying4;\nout vec3 nodeVarying5;\nout vec4 nodeVarying6;\n\n\n// attributes\nlayout( location = 0 ) in vec2 uv;\nlayout( location = 1 ) in vec3 normal;\nlayout( location = 2 ) in vec4 color;\nlayout( location = 3 ) in vec3 position;\n\n\n// vars\nmat4 modelViewMatrix;\nvec4 VERTEX_nodeVar3;\n\n// codes\n\n\nvoid main() {\n\n\t// transforms\n\t\n\n\t// flow\n\t// code\n\n\tnodeVarying4 = uv;\n\tnodeVarying5 = normal;\n\tnodeVarying6 = color;\n\tmodelViewMatrix = ( cameraViewMatrix * nodeUniform4 );\n\tpositionLocal = position;\n\tv_positionView = ( modelViewMatrix * vec4( positionLocal, 1.0 ) ).xyz;\n\tVERTEX_nodeVar3 = ( cameraProjectionMatrix * vec4( v_positionView, 1.0 ) );\n\tVERTEX_v_modelViewProjection = VERTEX_nodeVar3;\n\n\t// result\n\tgl_Position = VERTEX_v_modelViewProjection;\n\n\tgl_PointSize = 1.0;\n\n}\n",
  "fragment": "#version 300 es\n\n// Three.js r185 - Node System\n\n\n// extensions\n\n\n// precision\n\nprecision highp float;\nprecision highp int;\nprecision highp sampler2D;\nprecision highp sampler3D;\nprecision highp samplerCube;\nprecision highp sampler2DArray;\n\nprecision highp usampler2D;\nprecision highp usampler3D;\nprecision highp usamplerCube;\nprecision highp usampler2DArray;\n\nprecision highp isampler2D;\nprecision highp isampler3D;\nprecision highp isamplerCube;\nprecision highp isampler2DArray;\n\nprecision highp sampler2DShadow;\nprecision highp sampler2DArrayShadow;\nprecision highp samplerCubeShadow;\n\n\n// structs\n\nlayout( location = 0 ) out vec4 fragColor;\n\n\n\n// uniforms\n\nlayout( std140 ) uniform object {\n\tvec4 chaos;\n\tvec4 light;\n\tmat4 nodeUniform4;\n};\n\n\n// varyings\nin vec2 nodeVarying4;\nin vec3 nodeVarying5;\nin vec4 nodeVarying6;\n\n\n// vars\nfloat nodeVar0;\nfloat nodeVar1;\nfloat nodeVar2;\nvec3 normalLocal;\n\n// codes\n\n\nvoid main() {\n\n\t// flow\n\t// code\n\n\tnodeVar0 = ( 0.05 + ( clamp( ( ( nodeVarying4.y * 0.5 ) + 0.5 ), 0.0, 1.0 ) * 0.9 ) );\n\n\tfor ( int i = 0; i < 32; i ++ ) {\n\n\t\tnodeVar1 = ( chaos.x + ( ( chaos.y - chaos.x ) * clamp( ( ( nodeVarying4.x * 0.5 ) + 0.5 ), 0.0, 1.0 ) ) );\n\t\tnodeVar0 = ( ( nodeVar1 * nodeVar0 ) * ( 1.0 - nodeVar0 ) );\n\n\t}\n\n\tnodeVar2 = 0.0;\n\n\tfor ( int i = 0; i < 96; i ++ ) {\n\n\t\tnodeVar2 = ( nodeVar2 + log( abs( ( nodeVar1 * ( 1.0 - ( nodeVar0 * 2.0 ) ) ) ) ) );\n\t\tnodeVar0 = ( ( nodeVar1 * nodeVar0 ) * ( 1.0 - nodeVar0 ) );\n\n\t}\n\n\tnormalLocal = nodeVarying5;\n\n\t// result\n\tfragColor = vec4( ( ( mix( vec3( 0.08, 0.06, 0.2 ), vec3( 0.35, 0.95, 0.85 ), max( clamp( ( ( nodeVar2 / 96.0 ) / 0.6931471805599453 ), -1.0, 1.0 ), 0.0 ) ) * vec3( ( light.w + ( ( 1.0 - light.w ) * max( dot( normalize( normalLocal ), normalize( light.xyz ) ), 0.0 ) ) ) ) ) * nodeVarying6.xyz ), nodeVarying6.w );\n\n}\n"
 },
 "transplanted": {
  "wgsl": "// transplanted into the lyapunov look shell from three's WGSL node builder by render/tslSource.mjs\nstruct Cam { viewProj: mat4x4<f32>, light: vec4<f32>, chaos: vec4<f32> };\n@group(0) @binding(0) var<uniform> cam: Cam;\nfn turned(p: vec3<f32>, yaw: f32) -> vec3<f32> {\n  let ca = cos(yaw); let sa = sin(yaw);\n  return vec3<f32>(p.x * ca - p.y * sa, p.x * sa + p.y * ca, p.z);\n}\nstruct VOut { @builtin(position) pos: vec4<f32>, @location(0) color: vec4<f32>, @location(1) n: vec3<f32>, @location(2) local: vec2<f32> };\n@vertex fn vs(@location(0) p: vec3<f32>, @location(1) color: vec4<f32>, @location(2) rec: vec4<f32>, @location(3) ident: vec4<f32>, @location(5) extra: vec4<f32>, @location(4) n: vec3<f32>) -> VOut {\n  var o: VOut; var pl = p; var nl = n;\n  \n  o.pos = cam.viewProj * vec4<f32>(rec.xyz + turned(pl, extra.x) * rec.w, 1.0);\n  o.color = color; o.n = turned(n, extra.x); o.local = p.xy;\n  return o;\n}\n\n@fragment fn fs(v: VOut) -> @location(0) vec4<f32> {var nodeVar0 : f32;\nvar nodeVar1 : f32;\nvar nodeVar2 : f32;\nvar normalLocal : vec3<f32>;\n\n\n\t// flow\n\t// code\n\n\tnodeVar0 = ( 0.05 + ( clamp( ( ( v.local.y * 0.5 ) + 0.5 ), 0.0, 1.0 ) * 0.9 ) );\n\n\tfor ( var i : i32 = 0; i < 32; i ++ ) {\n\n\t\tnodeVar1 = ( cam.chaos.x + ( ( cam.chaos.y - cam.chaos.x ) * clamp( ( ( v.local.x * 0.5 ) + 0.5 ), 0.0, 1.0 ) ) );\n\t\tnodeVar0 = ( ( nodeVar1 * nodeVar0 ) * ( 1.0 - nodeVar0 ) );\n\n\t}\n\n\tnodeVar2 = 0.0;\n\n\tfor ( var i : i32 = 0; i < 96; i ++ ) {\n\n\t\tnodeVar2 = ( nodeVar2 + log( abs( ( nodeVar1 * ( 1.0 - ( nodeVar0 * 2.0 ) ) ) ) ) );\n\t\tnodeVar0 = ( ( nodeVar1 * nodeVar0 ) * ( 1.0 - nodeVar0 ) );\n\n\t}\n\n\tnormalLocal = v.n;\n\n\t// result\n\n\treturn vec4<f32>( ( ( mix( vec3<f32>( 0.08, 0.06, 0.2 ), vec3<f32>( 0.35, 0.95, 0.85 ), max( clamp( ( ( nodeVar2 / 96.0 ) / 0.6931471805599453 ), -1.0, 1.0 ), 0.0 ) ) * vec3<f32>( ( cam.light.w + ( ( 1.0 - cam.light.w ) * max( dot( normalize( normalLocal ), normalize( cam.light.xyz ) ), 0.0 ) ) ) ) ) * v.color.xyz ), v.color.w );\n\n}\n",
  "glsl": "#version 300 es\nprecision highp float;\nuniform mat4 viewProj; uniform vec4 light; uniform vec4 chaos;\nin vec4 vColor; in vec3 vN; in vec2 vLocal; out vec4 fragColor;\n\nvoid main() {float nodeVar0;\nfloat nodeVar1;\nfloat nodeVar2;\nvec3 normalLocal;\n\n\n\t// flow\n\t// code\n\n\tnodeVar0 = ( 0.05 + ( clamp( ( ( vLocal.y * 0.5 ) + 0.5 ), 0.0, 1.0 ) * 0.9 ) );\n\n\tfor ( int i = 0; i < 32; i ++ ) {\n\n\t\tnodeVar1 = ( chaos.x + ( ( chaos.y - chaos.x ) * clamp( ( ( vLocal.x * 0.5 ) + 0.5 ), 0.0, 1.0 ) ) );\n\t\tnodeVar0 = ( ( nodeVar1 * nodeVar0 ) * ( 1.0 - nodeVar0 ) );\n\n\t}\n\n\tnodeVar2 = 0.0;\n\n\tfor ( int i = 0; i < 96; i ++ ) {\n\n\t\tnodeVar2 = ( nodeVar2 + log( abs( ( nodeVar1 * ( 1.0 - ( nodeVar0 * 2.0 ) ) ) ) ) );\n\t\tnodeVar0 = ( ( nodeVar1 * nodeVar0 ) * ( 1.0 - nodeVar0 ) );\n\n\t}\n\n\tnormalLocal = vN;\n\n\t// result\n\tfragColor = vec4( ( ( mix( vec3( 0.08, 0.06, 0.2 ), vec3( 0.35, 0.95, 0.85 ), max( clamp( ( ( nodeVar2 / 96.0 ) / 0.6931471805599453 ), -1.0, 1.0 ), 0.0 ) ) * vec3( ( light.w + ( ( 1.0 - light.w ) * max( dot( normalize( normalLocal ), normalize( light.xyz ) ), 0.0 ) ) ) ) ) * vColor.xyz ), vColor.w );\n\n}\n"
 },
 "sprite": {
  "note": "v4325 -- the Heidler sprite look as three's builders emitted it, and as tslSource transplanted it into the sprite shell",
  "wgsl": {
   "language": "wgsl",
   "vertex": "// Three.js r185 - Node System\n\n// directives\n\n\n// structs\n\n\n// uniforms\n\nstruct renderStruct {\n\tcameraProjectionMatrix : mat4x4<f32>,\n\tcameraViewMatrix : mat4x4<f32>\n};\n@binding( 0 ) @group( 0 )\nvar<uniform> render : renderStruct;\n\nstruct objectStruct {\n\tbolt : vec4<f32>,\n\tspan : vec4<f32>,\n\tnodeUniform4 : mat4x4<f32>\n};\n@binding( 0 ) @group( 1 )\nvar<uniform> object : objectStruct;\n\n// varyings\n\nstruct VaryingsStruct {\n\t@location( 0 ) nodeVarying4 : vec2<f32>,\n\t@location( 1 ) nodeVarying5 : vec4<f32>,\n\t@builtin( position ) builtinClipSpace : vec4<f32>\n};\nvar<private> varyings : VaryingsStruct;\n\n// vars\nvar<private> modelViewMatrix : mat4x4<f32>;\nvar<private> VERTEX_nodeVar3 : vec4<f32>;\nvar<private> v_modelViewProjection : vec4<f32>;\nvar<private> v_positionView : vec3<f32>;\nvar<private> positionLocal : vec3<f32>;\nvar<private> VERTEX_v_modelViewProjection : vec4<f32>;\n\n// codes\n\n\n@vertex\nfn main( @location( 0 ) uv : vec2<f32>,\n\t@location( 1 ) color : vec4<f32>,\n\t@location( 2 ) position : vec3<f32> ) -> VaryingsStruct {\n\n\t// flow\n\t// code\n\n\tvaryings.nodeVarying4 = uv;\n\tvaryings.nodeVarying5 = color;\n\tmodelViewMatrix = ( render.cameraViewMatrix * object.nodeUniform4 );\n\tpositionLocal = position;\n\tv_positionView = ( modelViewMatrix * vec4<f32>( positionLocal, 1.0 ) ).xyz;\n\tVERTEX_nodeVar3 = ( render.cameraProjectionMatrix * vec4<f32>( v_positionView, 1.0 ) );\n\tVERTEX_v_modelViewProjection = VERTEX_nodeVar3;\n\n\t// result\n\n\tvaryings.builtinClipSpace = VERTEX_v_modelViewProjection;\n\n\treturn varyings;\n\n}\n",
   "fragment": "// Three.js r185 - Node System\n\n// global\ndiagnostic( off, derivative_uniformity );\n\n\n// structs\n\nstruct OutputStruct {\n\t@location( 0 ) color: vec4<f32>\n};\nvar<private> output : OutputStruct;\n\n// uniforms\n\nstruct objectStruct {\n\tbolt : vec4<f32>,\n\tspan : vec4<f32>,\n\tnodeUniform4 : mat4x4<f32>\n};\n@binding( 0 ) @group( 1 )\nvar<uniform> object : objectStruct;\n\n// vars\nvar<private> nodeVar0 : f32;\nvar<private> nodeVar1 : f32;\nvar<private> nodeVar2 : f32;\n\n// codes\n\n\n@fragment\nfn main( @location( 0 ) nodeVarying4 : vec2<f32>,\n\t@location( 1 ) nodeVarying5 : vec4<f32> ) -> OutputStruct {\n\n\t// flow\n\t// code\n\n\tnodeVar1 = ( object.span.x * exp( ( log( ( object.span.y / object.span.x ) ) * nodeVarying4.x ) ) );\n\n\tif ( ( nodeVar1 <= 0.0 ) ) {\n\n\t\tnodeVar0 = 0.0;\n\n\t} else {\n\n\t\tnodeVar2 = ( ( nodeVar1 / object.bolt.y ) * ( nodeVar1 / object.bolt.y ) );\n\t\tnodeVar0 = ( ( nodeVar2 / ( 1.0 + nodeVar2 ) ) * exp( ( ( - nodeVar1 ) / object.bolt.z ) ) );\n\n\t}\n\n\n\t// result\n\n\toutput.color = vec4<f32>( ( mix( vec3<f32>( 0.05, 0.05, 0.16 ), vec3<f32>( 1.0, 0.95, 0.7 ), ( clamp( ( ( ( object.bolt.x / object.bolt.w ) * nodeVar0 ) / object.bolt.x ), 0.0, 1.0 ) * clamp( ( 1.0 - abs( ( ( nodeVarying4.y * 2.0 ) - 1.0 ) ) ), 0.0, 1.0 ) ) ) * nodeVarying5.xyz ), nodeVarying5.w );\n\n\treturn output;\n\n}\n"
  },
  "glsl": {
   "language": "glsl",
   "vertex": "#version 300 es\n\n// Three.js r185 - Node System\n\n\n// extensions\n\n\n// precision\n\nprecision highp float;\nprecision highp int;\nprecision highp sampler2D;\nprecision highp sampler3D;\nprecision highp samplerCube;\nprecision highp sampler2DArray;\n\nprecision highp usampler2D;\nprecision highp usampler3D;\nprecision highp usamplerCube;\nprecision highp usampler2DArray;\n\nprecision highp isampler2D;\nprecision highp isampler3D;\nprecision highp isamplerCube;\nprecision highp isampler2DArray;\n\nprecision highp sampler2DShadow;\nprecision highp sampler2DArrayShadow;\nprecision highp samplerCubeShadow;\n\n\n// structs\n\n\n\n\n\n// uniforms\n\nlayout( std140 ) uniform render {\n\tmat4 cameraProjectionMatrix;\n\tmat4 cameraViewMatrix;\n};\n\nlayout( std140 ) uniform object {\n\tvec4 bolt;\n\tvec4 span;\n\tmat4 nodeUniform4;\n};\n\n\n// varyings\nvec4 v_modelViewProjection;\nvec3 v_positionView;\nvec3 positionLocal;\nvec4 VERTEX_v_modelViewProjection;\nout vec2 nodeVarying4;\nout vec4 nodeVarying5;\n\n\n// attributes\nlayout( location = 0 ) in vec2 uv;\nlayout( location = 1 ) in vec4 color;\nlayout( location = 2 ) in vec3 position;\n\n\n// vars\nmat4 modelViewMatrix;\nvec4 VERTEX_nodeVar3;\n\n// codes\n\n\nvoid main() {\n\n\t// transforms\n\t\n\n\t// flow\n\t// code\n\n\tnodeVarying4 = uv;\n\tnodeVarying5 = color;\n\tmodelViewMatrix = ( cameraViewMatrix * nodeUniform4 );\n\tpositionLocal = position;\n\tv_positionView = ( modelViewMatrix * vec4( positionLocal, 1.0 ) ).xyz;\n\tVERTEX_nodeVar3 = ( cameraProjectionMatrix * vec4( v_positionView, 1.0 ) );\n\tVERTEX_v_modelViewProjection = VERTEX_nodeVar3;\n\n\t// result\n\tgl_Position = VERTEX_v_modelViewProjection;\n\n\tgl_PointSize = 1.0;\n\n}\n",
   "fragment": "#version 300 es\n\n// Three.js r185 - Node System\n\n\n// extensions\n\n\n// precision\n\nprecision highp float;\nprecision highp int;\nprecision highp sampler2D;\nprecision highp sampler3D;\nprecision highp samplerCube;\nprecision highp sampler2DArray;\n\nprecision highp usampler2D;\nprecision highp usampler3D;\nprecision highp usamplerCube;\nprecision highp usampler2DArray;\n\nprecision highp isampler2D;\nprecision highp isampler3D;\nprecision highp isamplerCube;\nprecision highp isampler2DArray;\n\nprecision highp sampler2DShadow;\nprecision highp sampler2DArrayShadow;\nprecision highp samplerCubeShadow;\n\n\n// structs\n\nlayout( location = 0 ) out vec4 fragColor;\n\n\n\n// uniforms\n\nlayout( std140 ) uniform object {\n\tvec4 bolt;\n\tvec4 span;\n\tmat4 nodeUniform4;\n};\n\n\n// varyings\nin vec2 nodeVarying4;\nin vec4 nodeVarying5;\n\n\n// vars\nfloat nodeVar0;\nfloat nodeVar1;\nfloat nodeVar2;\n\n// codes\n\n\nvoid main() {\n\n\t// flow\n\t// code\n\n\tnodeVar1 = ( span.x * exp( ( log( ( span.y / span.x ) ) * nodeVarying4.x ) ) );\n\n\tif ( ( nodeVar1 <= 0.0 ) ) {\n\n\t\tnodeVar0 = 0.0;\n\n\t} else {\n\n\t\tnodeVar2 = ( ( nodeVar1 / bolt.y ) * ( nodeVar1 / bolt.y ) );\n\t\tnodeVar0 = ( ( nodeVar2 / ( 1.0 + nodeVar2 ) ) * exp( ( ( - nodeVar1 ) / bolt.z ) ) );\n\n\t}\n\n\n\t// result\n\tfragColor = vec4( ( mix( vec3( 0.05, 0.05, 0.16 ), vec3( 1.0, 0.95, 0.7 ), ( clamp( ( ( ( bolt.x / bolt.w ) * nodeVar0 ) / bolt.x ), 0.0, 1.0 ) * clamp( ( 1.0 - abs( ( ( nodeVarying4.y * 2.0 ) - 1.0 ) ) ), 0.0, 1.0 ) ) ) * nodeVarying5.xyz ), nodeVarying5.w );\n\n}\n"
  },
  "transplanted": {
   "wgsl": "// transplanted into the heidler sprite shell from three's WGSL node builder by render/tslSource.mjs\nstruct Cam { viewProj: mat4x4<f32>, bolt: vec4<f32>, span: vec4<f32> };\n@group(0) @binding(0) var<uniform> cam: Cam;\nfn turned(p: vec3<f32>, yaw: f32) -> vec3<f32> {\n  let ca = cos(yaw); let sa = sin(yaw);\n  return vec3<f32>(p.x * ca - p.y * sa, p.x * sa + p.y * ca, p.z);\n}\nstruct VOut { @builtin(position) pos: vec4<f32>, @location(0) color: vec4<f32>, @location(1) uv: vec2<f32> };\n@vertex fn vs(@location(0) p: vec3<f32>, @location(1) color: vec4<f32>, @location(2) rec: vec4<f32>, @location(3) ident: vec4<f32>, @location(5) extra: vec4<f32>, @location(4) uv: vec2<f32>) -> VOut {\n  var o: VOut; var pl = p;\n  \n  o.pos = cam.viewProj * vec4<f32>(rec.xyz + turned(pl, extra.x) * rec.w, 1.0);\n  o.color = color; o.uv = uv;\n  return o;\n}\n\n@fragment fn fs(v: VOut) -> @location(0) vec4<f32> {var nodeVar0 : f32;\nvar nodeVar1 : f32;\nvar nodeVar2 : f32;\n\n\n\t// flow\n\t// code\n\n\tnodeVar1 = ( cam.span.x * exp( ( log( ( cam.span.y / cam.span.x ) ) * v.uv.x ) ) );\n\n\tif ( ( nodeVar1 <= 0.0 ) ) {\n\n\t\tnodeVar0 = 0.0;\n\n\t} else {\n\n\t\tnodeVar2 = ( ( nodeVar1 / cam.bolt.y ) * ( nodeVar1 / cam.bolt.y ) );\n\t\tnodeVar0 = ( ( nodeVar2 / ( 1.0 + nodeVar2 ) ) * exp( ( ( - nodeVar1 ) / cam.bolt.z ) ) );\n\n\t}\n\n\n\t// result\n\n\treturn vec4<f32>( ( mix( vec3<f32>( 0.05, 0.05, 0.16 ), vec3<f32>( 1.0, 0.95, 0.7 ), ( clamp( ( ( ( cam.bolt.x / cam.bolt.w ) * nodeVar0 ) / cam.bolt.x ), 0.0, 1.0 ) * clamp( ( 1.0 - abs( ( ( v.uv.y * 2.0 ) - 1.0 ) ) ), 0.0, 1.0 ) ) ) * v.color.xyz ), v.color.w );\n\n}\n",
   "glsl": "#version 300 es\nprecision highp float;\nprecision highp sampler2D;\nuniform mat4 viewProj; uniform vec4 bolt; uniform vec4 span;\nin vec4 vColor; in vec2 vUv; out vec4 fragColor;\n\nvoid main() {float nodeVar0;\nfloat nodeVar1;\nfloat nodeVar2;\n\n\n\t// flow\n\t// code\n\n\tnodeVar1 = ( span.x * exp( ( log( ( span.y / span.x ) ) * vUv.x ) ) );\n\n\tif ( ( nodeVar1 <= 0.0 ) ) {\n\n\t\tnodeVar0 = 0.0;\n\n\t} else {\n\n\t\tnodeVar2 = ( ( nodeVar1 / bolt.y ) * ( nodeVar1 / bolt.y ) );\n\t\tnodeVar0 = ( ( nodeVar2 / ( 1.0 + nodeVar2 ) ) * exp( ( ( - nodeVar1 ) / bolt.z ) ) );\n\n\t}\n\n\n\t// result\n\tfragColor = vec4( ( mix( vec3( 0.05, 0.05, 0.16 ), vec3( 1.0, 0.95, 0.7 ), ( clamp( ( ( ( bolt.x / bolt.w ) * nodeVar0 ) / bolt.x ), 0.0, 1.0 ) * clamp( ( 1.0 - abs( ( ( vUv.y * 2.0 ) - 1.0 ) ) ), 0.0, 1.0 ) ) ) * vColor.xyz ), vColor.w );\n\n}\n"
  }
 },
 "ink": {
  "note": "v4328 -- the ink wash as three's builders emitted it, and as tslSource transplanted it into the fleets' line-list shell",
  "wgsl": {
   "language": "wgsl",
   "vertex": "// Three.js r185 - Node System\n\n// directives\n\n\n// structs\n\n\n// uniforms\n\nstruct renderStruct {\n\tcameraProjectionMatrix : mat4x4<f32>,\n\tcameraViewMatrix : mat4x4<f32>\n};\n@binding( 0 ) @group( 0 )\nvar<uniform> render : renderStruct;\n\nstruct objectStruct {\n\tink : vec4<f32>,\n\tnodeUniform3 : mat4x4<f32>\n};\n@binding( 0 ) @group( 1 )\nvar<uniform> object : objectStruct;\n\n// varyings\n\nstruct VaryingsStruct {\n\t@location( 0 ) nodeVarying4 : vec4<f32>,\n\t@builtin( position ) builtinClipSpace : vec4<f32>\n};\nvar<private> varyings : VaryingsStruct;\n\n// vars\nvar<private> modelViewMatrix : mat4x4<f32>;\nvar<private> VERTEX_nodeVar0 : vec4<f32>;\nvar<private> v_modelViewProjection : vec4<f32>;\nvar<private> v_positionView : vec3<f32>;\nvar<private> positionLocal : vec3<f32>;\nvar<private> VERTEX_v_modelViewProjection : vec4<f32>;\n\n// codes\n\n\n@vertex\nfn main( @location( 0 ) color : vec4<f32>,\n\t@location( 1 ) position : vec3<f32> ) -> VaryingsStruct {\n\n\t// flow\n\t// code\n\n\tvaryings.nodeVarying4 = color;\n\tmodelViewMatrix = ( render.cameraViewMatrix * object.nodeUniform3 );\n\tpositionLocal = position;\n\tv_positionView = ( modelViewMatrix * vec4<f32>( positionLocal, 1.0 ) ).xyz;\n\tVERTEX_nodeVar0 = ( render.cameraProjectionMatrix * vec4<f32>( v_positionView, 1.0 ) );\n\tVERTEX_v_modelViewProjection = VERTEX_nodeVar0;\n\n\t// result\n\n\tvaryings.builtinClipSpace = VERTEX_v_modelViewProjection;\n\n\treturn varyings;\n\n}\n",
   "fragment": "// Three.js r185 - Node System\n\n// global\ndiagnostic( off, derivative_uniformity );\n\n\n// structs\n\nstruct OutputStruct {\n\t@location( 0 ) color: vec4<f32>\n};\nvar<private> output : OutputStruct;\n\n// uniforms\n\nstruct objectStruct {\n\tink : vec4<f32>,\n\tnodeUniform3 : mat4x4<f32>\n};\n@binding( 0 ) @group( 1 )\nvar<uniform> object : objectStruct;\n\n// vars\n\n\n// codes\n\n\n@fragment\nfn main( @location( 0 ) nodeVarying4 : vec4<f32> ) -> OutputStruct {\n\n\t// flow\n\t// code\n\n\n\t// result\n\n\toutput.color = vec4<f32>( ( mix( nodeVarying4.xyz, vec3<f32>( dot( nodeVarying4.xyz, vec3<f32>( 0.299, 0.587, 0.114 ) ) ), object.ink.x ) * vec3<f32>( object.ink.y ) ), nodeVarying4.w );\n\n\treturn output;\n\n}\n"
  },
  "glsl": {
   "language": "glsl",
   "vertex": "#version 300 es\n\n// Three.js r185 - Node System\n\n\n// extensions\n\n\n// precision\n\nprecision highp float;\nprecision highp int;\nprecision highp sampler2D;\nprecision highp sampler3D;\nprecision highp samplerCube;\nprecision highp sampler2DArray;\n\nprecision highp usampler2D;\nprecision highp usampler3D;\nprecision highp usamplerCube;\nprecision highp usampler2DArray;\n\nprecision highp isampler2D;\nprecision highp isampler3D;\nprecision highp isamplerCube;\nprecision highp isampler2DArray;\n\nprecision highp sampler2DShadow;\nprecision highp sampler2DArrayShadow;\nprecision highp samplerCubeShadow;\n\n\n// structs\n\n\n\n\n\n// uniforms\n\nlayout( std140 ) uniform render {\n\tmat4 cameraProjectionMatrix;\n\tmat4 cameraViewMatrix;\n};\n\nlayout( std140 ) uniform object {\n\tvec4 ink;\n\tmat4 nodeUniform3;\n};\n\n\n// varyings\nvec4 v_modelViewProjection;\nvec3 v_positionView;\nvec3 positionLocal;\nvec4 VERTEX_v_modelViewProjection;\nout vec4 nodeVarying4;\n\n\n// attributes\nlayout( location = 0 ) in vec4 color;\nlayout( location = 1 ) in vec3 position;\n\n\n// vars\nmat4 modelViewMatrix;\nvec4 VERTEX_nodeVar0;\n\n// codes\n\n\nvoid main() {\n\n\t// transforms\n\t\n\n\t// flow\n\t// code\n\n\tnodeVarying4 = color;\n\tmodelViewMatrix = ( cameraViewMatrix * nodeUniform3 );\n\tpositionLocal = position;\n\tv_positionView = ( modelViewMatrix * vec4( positionLocal, 1.0 ) ).xyz;\n\tVERTEX_nodeVar0 = ( cameraProjectionMatrix * vec4( v_positionView, 1.0 ) );\n\tVERTEX_v_modelViewProjection = VERTEX_nodeVar0;\n\n\t// result\n\tgl_Position = VERTEX_v_modelViewProjection;\n\n\tgl_PointSize = 1.0;\n\n}\n",
   "fragment": "#version 300 es\n\n// Three.js r185 - Node System\n\n\n// extensions\n\n\n// precision\n\nprecision highp float;\nprecision highp int;\nprecision highp sampler2D;\nprecision highp sampler3D;\nprecision highp samplerCube;\nprecision highp sampler2DArray;\n\nprecision highp usampler2D;\nprecision highp usampler3D;\nprecision highp usamplerCube;\nprecision highp usampler2DArray;\n\nprecision highp isampler2D;\nprecision highp isampler3D;\nprecision highp isamplerCube;\nprecision highp isampler2DArray;\n\nprecision highp sampler2DShadow;\nprecision highp sampler2DArrayShadow;\nprecision highp samplerCubeShadow;\n\n\n// structs\n\nlayout( location = 0 ) out vec4 fragColor;\n\n\n\n// uniforms\n\nlayout( std140 ) uniform object {\n\tvec4 ink;\n\tmat4 nodeUniform3;\n};\n\n\n// varyings\nin vec4 nodeVarying4;\n\n\n// vars\n\n\n// codes\n\n\nvoid main() {\n\n\t// flow\n\t// code\n\n\n\t// result\n\tfragColor = vec4( ( mix( nodeVarying4.xyz, vec3( dot( nodeVarying4.xyz, vec3( 0.299, 0.587, 0.114 ) ) ), ink.x ) * vec3( ink.y ) ), nodeVarying4.w );\n\n}\n"
  },
  "transplanted": {
   "wgsl": "// transplanted into the ink shell from three's WGSL node builder by render/tslSource.mjs\nstruct Cam { viewProj: mat4x4<f32>, ink: vec4<f32> };\n@group(0) @binding(0) var<uniform> cam: Cam;\nfn turned(p: vec3<f32>, yaw: f32) -> vec3<f32> {\n  let ca = cos(yaw); let sa = sin(yaw);\n  return vec3<f32>(p.x * ca - p.y * sa, p.x * sa + p.y * ca, p.z);\n}\nstruct VOut { @builtin(position) pos: vec4<f32>, @location(0) color: vec4<f32> };\n@vertex fn vs(@location(0) p: vec3<f32>, @location(1) color: vec4<f32>, @location(2) rec: vec4<f32>, @location(3) ident: vec4<f32>, @location(5) extra: vec4<f32>) -> VOut {\n  var o: VOut; var pl = p;\n  \n  o.pos = cam.viewProj * vec4<f32>(rec.xyz + turned(pl, extra.x) * rec.w, 1.0);\n  o.color = color;\n  return o;\n}\n\n@fragment fn fs(v: VOut) -> @location(0) vec4<f32> {\n\n\t// flow\n\t// code\n\n\n\t// result\n\n\treturn vec4<f32>( ( mix( v.color.xyz, vec3<f32>( dot( v.color.xyz, vec3<f32>( 0.299, 0.587, 0.114 ) ) ), cam.ink.x ) * vec3<f32>( cam.ink.y ) ), v.color.w );\n\n}\n",
   "glsl": "#version 300 es\nprecision highp float;\nuniform mat4 viewProj; uniform vec4 ink;\nin vec4 vColor; out vec4 fragColor;\n\nvoid main() {\n\n\t// flow\n\t// code\n\n\n\t// result\n\tfragColor = vec4( ( mix( vColor.xyz, vec3( dot( vColor.xyz, vec3( 0.299, 0.587, 0.114 ) ) ), ink.x ) * vec3( ink.y ) ), vColor.w );\n\n}\n"
  }
 }
}
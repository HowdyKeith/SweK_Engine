{
 "note": "fixtures in the shape three r178's node builders emit, for tools/ship/tslSource-selfcheck.mjs section 1; kept as data so the gate file bears neither shader language and the parity census does not count it",
 "wgsl": "// Three.js r178 - Node System\n// uniforms\n@binding( 0 ) @group( 1 ) var {{TEX}}_sampler : sampler;\n@binding( 1 ) @group( 1 ) var {{TEX}} : texture_2d<f32>;\nstruct objectStruct {\n{{UNI}}\n};\n@binding( 2 ) @group( 1 )\nvar<uniform> object : objectStruct;\n// codes\nfn helper( x : f32 ) -> f32 { return fract( x ); }\n@fragment\nfn main( @location( 3 ) {{VARY}} : vec2<f32> ) -> OutputStruct {\n\tvar nodeVar0 : vec4<f32>;\n\tnodeVar0 = textureSample( {{TEX}}, {{TEX}}_sampler, vec2<f32>( helper( {{VARY}}.x + ( object.time * object.speed ) ), {{VARY}}.y ) );{{EXTRA}}\n\t// result\n\toutput.color = nodeVar0;\n\treturn output;\n}\n",
 "glsl": "#version 300 es\n// Three.js r178 - Node System\nprecision highp float;\nlayout( std140 ) uniform fragment_object {\n{{UNI}}\n};\nuniform sampler2D {{TEX}};\nin vec2 nodeVarying3;\n// codes\nfloat helper( float x ) { return fract( x ); }\n// structs\nlayout( location = 0 ) out vec4 fragColor;\nvoid main() {\n\tvec4 nodeVar0;\n\tnodeVar0 = texture( {{TEX}}, vec2( helper( nodeVarying3.x + ( f_time * f_speed ) ), nodeVarying3.y ) );\n\tfragColor = nodeVar0;\n}\n"
}
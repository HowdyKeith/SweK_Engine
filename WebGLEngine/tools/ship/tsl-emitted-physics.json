{
 "at": "v4321",
 "three": "0.178.0",
 "note": "emitted by three's node builders from render/physicsTsl.mjs and transplanted by render/tslSource.mjs; rewritten by tools/ship/tslPhysics-selfcheck.mjs on every run",
 "lyapunov": {
  "wgsl": "// Three.js r178 - Node System\n\n// global\ndiagnostic( off, derivative_uniformity );\n\n\n// structs\n\nstruct OutputStruct {\n\t@location(0) color: vec4<f32>\n};\nvar<private> output : OutputStruct;\n\n// uniforms\n\nstruct objectStruct {\n\tseedLo : f32,\n\tseedHi : f32,\n\trLo : f32,\n\trHi : f32\n};\n@binding( 0 ) @group( 1 )\nvar<uniform> object : objectStruct;\n\n// codes\n\n\n@fragment\nfn main( @location( 3 ) nodeVarying3 : vec2<f32> ) -> OutputStruct {\n\n\t// vars\n\t\n\tvar nodeVar0 : f32;\n\tvar nodeVar1 : f32;\n\tvar nodeVar2 : f32;\n\tvar nodeVar3 : f32;\n\n\n\t// flow\n\t// code\n\n\tnodeVar0 = ( object.seedLo + ( ( object.seedHi - object.seedLo ) * nodeVarying3.y ) );\n\n\tfor ( var i : i32 = 0; i < 64; i ++ ) {\n\n\t\tnodeVar1 = ( object.rLo + ( ( object.rHi - object.rLo ) * nodeVarying3.x ) );\n\t\tnodeVar0 = ( ( nodeVar1 * nodeVar0 ) * ( 1.0 - nodeVar0 ) );\n\n\t}\n\n\tnodeVar2 = 0.0;\n\n\tfor ( var i : i32 = 0; i < 384; i ++ ) {\n\n\t\tnodeVar2 = ( nodeVar2 + log( abs( ( nodeVar1 * ( 1.0 - ( nodeVar0 * 2.0 ) ) ) ) ) );\n\t\tnodeVar0 = ( ( nodeVar1 * nodeVar0 ) * ( 1.0 - nodeVar0 ) );\n\n\t}\n\n\tnodeVar3 = clamp( ( ( ( nodeVar2 / 384.0 ) + 3.0 ) / 4.0 ), 0.0, 1.0 );\n\n\t// result\n\n\toutput.color = vec4<f32>( ( floor( ( nodeVar3 * 255.0 ) ) / 255.0 ), fract( ( nodeVar3 * 255.0 ) ), 0.0, 1.0 );\n\n\treturn output;\n\n}\n",
  "glsl": "#version 300 es\n\n// Three.js r178 - Node System\n\n\n// extensions\n\n\n// precision\n\nprecision highp float;\nprecision highp int;\nprecision highp sampler2D;\nprecision highp sampler3D;\nprecision highp samplerCube;\nprecision highp sampler2DArray;\n\nprecision highp usampler2D;\nprecision highp usampler3D;\nprecision highp usamplerCube;\nprecision highp usampler2DArray;\n\nprecision highp isampler2D;\nprecision highp isampler3D;\nprecision highp isamplerCube;\nprecision highp isampler2DArray;\n\nprecision lowp sampler2DShadow;\nprecision lowp sampler2DArrayShadow;\nprecision lowp samplerCubeShadow;\n\n\n// uniforms\n\nlayout( std140 ) uniform fragment_object {\n\tfloat f_seedLo;\n\tfloat f_seedHi;\n\tfloat f_rLo;\n\tfloat f_rHi;\n};\n\n\n// varyings\nin vec2 nodeVarying3;\n\n\n// codes\n\n\n// structs\n\nlayout( location = 0 ) out vec4 fragColor;\n\n\n\nvoid main() {\n\n\t// vars\n\tfloat nodeVar0;\n\tfloat nodeVar1;\n\tfloat nodeVar2;\n\tfloat nodeVar3;\n\n\t// flow\n\t// code\n\n\tnodeVar0 = ( f_seedLo + ( ( f_seedHi - f_seedLo ) * nodeVarying3.y ) );\n\n\tfor ( int i = 0; i < 64; i ++ ) {\n\n\t\tnodeVar1 = ( f_rLo + ( ( f_rHi - f_rLo ) * nodeVarying3.x ) );\n\t\tnodeVar0 = ( ( nodeVar1 * nodeVar0 ) * ( 1.0 - nodeVar0 ) );\n\n\t}\n\n\tnodeVar2 = 0.0;\n\n\tfor ( int i = 0; i < 384; i ++ ) {\n\n\t\tnodeVar2 = ( nodeVar2 + log( abs( ( nodeVar1 * ( 1.0 - ( nodeVar0 * 2.0 ) ) ) ) ) );\n\t\tnodeVar0 = ( ( nodeVar1 * nodeVar0 ) * ( 1.0 - nodeVar0 ) );\n\n\t}\n\n\tnodeVar3 = clamp( ( ( ( nodeVar2 / 384.0 ) + 3.0 ) / 4.0 ), 0.0, 1.0 );\n\n\t// result\n\tfragColor = vec4( ( floor( ( nodeVar3 * 255.0 ) ) / 255.0 ), fract( ( nodeVar3 * 255.0 ) ), 0.0, 1.0 );\n\n}\n",
  "transplanted": {
   "wgsl": "// transplanted from three's WGSL node builder by render/tslSource.mjs\nstruct U { seedLo: f32, seedHi: f32, rLo: f32, rHi: f32 };\n@group(0) @binding(0) var<uniform> u: U;\n\nstruct VSOut { @builtin(position) pos: vec4f, @location(0) uv: vec2f };\n@vertex fn vs(@builtin(vertex_index) vi: u32) -> VSOut {\n  var p = array<vec2f, 3>(vec2f(-1.0, -1.0), vec2f(3.0, -1.0), vec2f(-1.0, 3.0));\n  var o: VSOut; o.pos = vec4f(p[vi], 0.0, 1.0); o.uv = vec2f((p[vi].x + 1.0) * 0.5, 1.0 - (p[vi].y + 1.0) * 0.5); return o;\n}\n\n@fragment fn fs(@location(0) uv: vec2f) -> @location(0) vec4f {\n\n\t// vars\n\t\n\tvar nodeVar0 : f32;\n\tvar nodeVar1 : f32;\n\tvar nodeVar2 : f32;\n\tvar nodeVar3 : f32;\n\n\n\t// flow\n\t// code\n\n\tnodeVar0 = ( u.seedLo + ( ( u.seedHi - u.seedLo ) * uv.y ) );\n\n\tfor ( var i : i32 = 0; i < 64; i ++ ) {\n\n\t\tnodeVar1 = ( u.rLo + ( ( u.rHi - u.rLo ) * uv.x ) );\n\t\tnodeVar0 = ( ( nodeVar1 * nodeVar0 ) * ( 1.0 - nodeVar0 ) );\n\n\t}\n\n\tnodeVar2 = 0.0;\n\n\tfor ( var i : i32 = 0; i < 384; i ++ ) {\n\n\t\tnodeVar2 = ( nodeVar2 + log( abs( ( nodeVar1 * ( 1.0 - ( nodeVar0 * 2.0 ) ) ) ) ) );\n\t\tnodeVar0 = ( ( nodeVar1 * nodeVar0 ) * ( 1.0 - nodeVar0 ) );\n\n\t}\n\n\tnodeVar3 = clamp( ( ( ( nodeVar2 / 384.0 ) + 3.0 ) / 4.0 ), 0.0, 1.0 );\n\n\t// result\n\n\treturn vec4<f32>( ( floor( ( nodeVar3 * 255.0 ) ) / 255.0 ), fract( ( nodeVar3 * 255.0 ) ), 0.0, 1.0 );\n\n}\n",
   "glsl": "#version 300 es\n// transplanted from three's GLSL node builder by render/tslSource.mjs\nprecision highp float;\nprecision highp int;\nprecision highp sampler2D;\nuniform float seedLo;\nuniform float seedHi;\nuniform float rLo;\nuniform float rHi;\n\nin vec2 vUv;\nout vec4 fragColor;\n\nvoid main() {\n\n\t// vars\n\tfloat nodeVar0;\n\tfloat nodeVar1;\n\tfloat nodeVar2;\n\tfloat nodeVar3;\n\n\t// flow\n\t// code\n\n\tnodeVar0 = ( seedLo + ( ( seedHi - seedLo ) * vUv.y ) );\n\n\tfor ( int i = 0; i < 64; i ++ ) {\n\n\t\tnodeVar1 = ( rLo + ( ( rHi - rLo ) * vUv.x ) );\n\t\tnodeVar0 = ( ( nodeVar1 * nodeVar0 ) * ( 1.0 - nodeVar0 ) );\n\n\t}\n\n\tnodeVar2 = 0.0;\n\n\tfor ( int i = 0; i < 384; i ++ ) {\n\n\t\tnodeVar2 = ( nodeVar2 + log( abs( ( nodeVar1 * ( 1.0 - ( nodeVar0 * 2.0 ) ) ) ) ) );\n\t\tnodeVar0 = ( ( nodeVar1 * nodeVar0 ) * ( 1.0 - nodeVar0 ) );\n\n\t}\n\n\tnodeVar3 = clamp( ( ( ( nodeVar2 / 384.0 ) + 3.0 ) / 4.0 ), 0.0, 1.0 );\n\n\t// result\n\tfragColor = vec4( ( floor( ( nodeVar3 * 255.0 ) ) / 255.0 ), fract( ( nodeVar3 * 255.0 ) ), 0.0, 1.0 );\n\n}\n"
  }
 },
 "heidler": {
  "wgsl": "// Three.js r178 - Node System\n\n// global\ndiagnostic( off, derivative_uniformity );\n\n\n// structs\n\nstruct OutputStruct {\n\t@location(0) color: vec4<f32>\n};\nvar<private> output : OutputStruct;\n\n// uniforms\n\nstruct objectStruct {\n\ti0 : f32,\n\teta : f32,\n\ttLo : f32,\n\ttHi : f32,\n\tt1 : f32,\n\tt2 : f32\n};\n@binding( 0 ) @group( 1 )\nvar<uniform> object : objectStruct;\n\n// codes\n\n\n@fragment\nfn main( @location( 3 ) nodeVarying3 : vec2<f32> ) -> OutputStruct {\n\n\t// vars\n\t\n\tvar nodeVar0 : f32;\n\tvar nodeVar1 : f32;\n\tvar nodeVar2 : f32;\n\tvar nodeVar3 : f32;\n\n\n\t// flow\n\t// code\n\n\n\tif ( ( ( object.tLo * exp( ( log( ( object.tHi / object.tLo ) ) * nodeVarying3.x ) ) ) <= 0.0 ) ) {\n\n\t\tnodeVar0 = 0.0;\n\n\t} else {\n\n\t\tnodeVar1 = ( object.tLo * exp( ( log( ( object.tHi / object.tLo ) ) * nodeVarying3.x ) ) );\n\t\tnodeVar2 = ( ( nodeVar1 / object.t1 ) * ( nodeVar1 / object.t1 ) );\n\t\tnodeVar0 = ( ( nodeVar2 / ( 1.0 + nodeVar2 ) ) * exp( ( ( - nodeVar1 ) / object.t2 ) ) );\n\n\t}\n\n\tnodeVar3 = clamp( ( ( ( ( object.i0 / object.eta ) * nodeVar0 ) / object.i0 ) / 2.0 ), 0.0, 1.0 );\n\n\t// result\n\n\toutput.color = vec4<f32>( ( floor( ( nodeVar3 * 255.0 ) ) / 255.0 ), fract( ( nodeVar3 * 255.0 ) ), 0.0, 1.0 );\n\n\treturn output;\n\n}\n",
  "glsl": "#version 300 es\n\n// Three.js r178 - Node System\n\n\n// extensions\n\n\n// precision\n\nprecision highp float;\nprecision highp int;\nprecision highp sampler2D;\nprecision highp sampler3D;\nprecision highp samplerCube;\nprecision highp sampler2DArray;\n\nprecision highp usampler2D;\nprecision highp usampler3D;\nprecision highp usamplerCube;\nprecision highp usampler2DArray;\n\nprecision highp isampler2D;\nprecision highp isampler3D;\nprecision highp isamplerCube;\nprecision highp isampler2DArray;\n\nprecision lowp sampler2DShadow;\nprecision lowp sampler2DArrayShadow;\nprecision lowp samplerCubeShadow;\n\n\n// uniforms\n\nlayout( std140 ) uniform fragment_object {\n\tfloat f_i0;\n\tfloat f_eta;\n\tfloat f_tLo;\n\tfloat f_tHi;\n\tfloat f_t1;\n\tfloat f_t2;\n};\n\n\n// varyings\nin vec2 nodeVarying3;\n\n\n// codes\n\n\n// structs\n\nlayout( location = 0 ) out vec4 fragColor;\n\n\n\nvoid main() {\n\n\t// vars\n\tfloat nodeVar0;\n\tfloat nodeVar1;\n\tfloat nodeVar2;\n\tfloat nodeVar3;\n\n\t// flow\n\t// code\n\n\n\tif ( ( ( f_tLo * exp( ( log( ( f_tHi / f_tLo ) ) * nodeVarying3.x ) ) ) <= 0.0 ) ) {\n\n\t\tnodeVar0 = 0.0;\n\n\t} else {\n\n\t\tnodeVar1 = ( f_tLo * exp( ( log( ( f_tHi / f_tLo ) ) * nodeVarying3.x ) ) );\n\t\tnodeVar2 = ( ( nodeVar1 / f_t1 ) * ( nodeVar1 / f_t1 ) );\n\t\tnodeVar0 = ( ( nodeVar2 / ( 1.0 + nodeVar2 ) ) * exp( ( ( - nodeVar1 ) / f_t2 ) ) );\n\n\t}\n\n\tnodeVar3 = clamp( ( ( ( ( f_i0 / f_eta ) * nodeVar0 ) / f_i0 ) / 2.0 ), 0.0, 1.0 );\n\n\t// result\n\tfragColor = vec4( ( floor( ( nodeVar3 * 255.0 ) ) / 255.0 ), fract( ( nodeVar3 * 255.0 ) ), 0.0, 1.0 );\n\n}\n",
  "transplanted": {
   "wgsl": "// transplanted from three's WGSL node builder by render/tslSource.mjs\nstruct U { i0: f32, eta: f32, tLo: f32, tHi: f32, t1: f32, t2: f32 };\n@group(0) @binding(0) var<uniform> u: U;\n\nstruct VSOut { @builtin(position) pos: vec4f, @location(0) uv: vec2f };\n@vertex fn vs(@builtin(vertex_index) vi: u32) -> VSOut {\n  var p = array<vec2f, 3>(vec2f(-1.0, -1.0), vec2f(3.0, -1.0), vec2f(-1.0, 3.0));\n  var o: VSOut; o.pos = vec4f(p[vi], 0.0, 1.0); o.uv = vec2f((p[vi].x + 1.0) * 0.5, 1.0 - (p[vi].y + 1.0) * 0.5); return o;\n}\n\n@fragment fn fs(@location(0) uv: vec2f) -> @location(0) vec4f {\n\n\t// vars\n\t\n\tvar nodeVar0 : f32;\n\tvar nodeVar1 : f32;\n\tvar nodeVar2 : f32;\n\tvar nodeVar3 : f32;\n\n\n\t// flow\n\t// code\n\n\n\tif ( ( ( u.tLo * exp( ( log( ( u.tHi / u.tLo ) ) * uv.x ) ) ) <= 0.0 ) ) {\n\n\t\tnodeVar0 = 0.0;\n\n\t} else {\n\n\t\tnodeVar1 = ( u.tLo * exp( ( log( ( u.tHi / u.tLo ) ) * uv.x ) ) );\n\t\tnodeVar2 = ( ( nodeVar1 / u.t1 ) * ( nodeVar1 / u.t1 ) );\n\t\tnodeVar0 = ( ( nodeVar2 / ( 1.0 + nodeVar2 ) ) * exp( ( ( - nodeVar1 ) / u.t2 ) ) );\n\n\t}\n\n\tnodeVar3 = clamp( ( ( ( ( u.i0 / u.eta ) * nodeVar0 ) / u.i0 ) / 2.0 ), 0.0, 1.0 );\n\n\t// result\n\n\treturn vec4<f32>( ( floor( ( nodeVar3 * 255.0 ) ) / 255.0 ), fract( ( nodeVar3 * 255.0 ) ), 0.0, 1.0 );\n\n}\n",
   "glsl": "#version 300 es\n// transplanted from three's GLSL node builder by render/tslSource.mjs\nprecision highp float;\nprecision highp int;\nprecision highp sampler2D;\nuniform float i0;\nuniform float eta;\nuniform float tLo;\nuniform float tHi;\nuniform float t1;\nuniform float t2;\n\nin vec2 vUv;\nout vec4 fragColor;\n\nvoid main() {\n\n\t// vars\n\tfloat nodeVar0;\n\tfloat nodeVar1;\n\tfloat nodeVar2;\n\tfloat nodeVar3;\n\n\t// flow\n\t// code\n\n\n\tif ( ( ( tLo * exp( ( log( ( tHi / tLo ) ) * vUv.x ) ) ) <= 0.0 ) ) {\n\n\t\tnodeVar0 = 0.0;\n\n\t} else {\n\n\t\tnodeVar1 = ( tLo * exp( ( log( ( tHi / tLo ) ) * vUv.x ) ) );\n\t\tnodeVar2 = ( ( nodeVar1 / t1 ) * ( nodeVar1 / t1 ) );\n\t\tnodeVar0 = ( ( nodeVar2 / ( 1.0 + nodeVar2 ) ) * exp( ( ( - nodeVar1 ) / t2 ) ) );\n\n\t}\n\n\tnodeVar3 = clamp( ( ( ( ( i0 / eta ) * nodeVar0 ) / i0 ) / 2.0 ), 0.0, 1.0 );\n\n\t// result\n\tfragColor = vec4( ( floor( ( nodeVar3 * 255.0 ) ) / 255.0 ), fract( ( nodeVar3 * 255.0 ) ), 0.0, 1.0 );\n\n}\n"
  }
 }
}
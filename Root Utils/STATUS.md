# STATUS.md — SweK Engine current state

_Auto-generated by `tools/ship/status.mjs` on 2026-07-29. This is the LIVE state;
read `SESSION_START.md` for the session protocol. Regenerated on every ship, so it should not be stale._

## Current version
**v3200** — the newest shipped build. Work forward from here; never reuse a version number (supersede forward).

## Recently shipped
- **v3200** — 
- **v3199** — 
- **v3198** — 
- **v3197** — 
- **v3196** — 
- **v3195** — 

## Rig-pending (needs Keith's actual hardware — not verifiable from the sandbox)
These are auto-extracted from recent changelog entries. Claude builds them "correct by construction" but the
listed step happens on Keith's boxes (Galaxina = Windows+GPU, Stellar Atlas = Intel Mac). Check them off as done.
- [ ] (none flagged in the recent window)

## Next
- **?seed / ?cam / ?preset deterministic repro** — URL params that set world seed / camera / preset so a
  state reproduces from a link. Synergises with render-QA (stable baselines). Medium engine change.
- **render-QA -> ship gate** — opt-in `verify.mjs --render-qa` that fails the build on a visual regression.
  Deferred until Keith has trusted baselines from a Galaxina run.
- **THREE-NOTES.md** — verified three.js API notes per version (low value; SweK is mostly hand-rolled WebGL2).
